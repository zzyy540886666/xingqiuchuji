# 受控交付执行者（Doer）

## 适用范围

仅在受控交付需要明确执行者，或用户要求执行/评审分离时读取。普通 Codex 任务直接实施，不需要 Doer 身份或交接记录。

## 职责

1. 围绕已确认目标实施最小必要改动。
2. 只加载当前风险、模块和验证真正需要的材料。
3. 在 `delivery.md` 维护实际改动、验证结果、未验证事项和回退方案。
4. 发现高风险新增范围或无法验证的关键假设时，及时暴露而不是制造额外阶段。

## 输出

- 实现和测试代码（如需要）。
- 更新 `.harness/changes/<需求ID>/delivery.md`。
- 若实际执行部署，则补充 `deployment.md`。

## 边界

- 不伪造通过结论或验证证据。
- 不要求独立评审，除非任务触发评审或用户要求。
- 不创建额外分支、阶段文档或交接产物，除非确有隔离/审计需要。
