# 用户 2063223234097950700 宇树业务模拟数据交付

## 目标与范围

为用户 `2063223234097950700` 创建可在小程序“我的”页面及八个入口中联动展示的宇树主题模拟数据，覆盖订单、钱包、会员、资产、托管收益、分销、报修和消息。

## 风险触点与关键决策

- 涉及订单、支付、钱包和佣金，所有金额使用整数分。
- 以运行中的 `xingqiu_dev` 表结构和后端查询实现为准。
- 使用固定业务 ID 和 `INSERT IGNORE` / `ON DUPLICATE KEY UPDATE`，保证脚本可重复执行。
- 订单、支付、会员累计消费、资产来源、收益流水、佣金来源订单及钱包流水保持可追溯关系。
- 目标用户在实施前不存在，因此脚本先创建用户；仅使用演示 openid，不包含真实敏感信息。

## 实际改动

- 新增 `backend/src/main/resources/seed-user-2063223234097950700-unitree.sql`。
- 数据范围：
  - 5 个目标用户订单，覆盖已完成、履约中、待支付、已取消。
  - 订单行、状态事件和支付记录。
  - Lv.3 会员、星球卡有效期、三类会员权益及会员事件。
  - Go2 Edu、B2、G1 三台宇树资产，覆盖空闲、托管中、维护中。
  - 托管时段和已结算/待结算收益。
  - 两个一级成员、一个二级成员、已结算和保护期佣金。
  - 钱包余额、冻结金额、完整流水和处理中提现。
  - 三台对应设备、三种状态的报修工单及进度日志。
  - 八条订单、系统、互动通知，其中五条未读。

## 验证证据与结果

- SQL 已以 UTF-8 导入当前 `xq-mysql/xingqiu_dev`，并再次执行验证幂等性；两次执行后汇总一致。
- “我的”页面关联汇总：
  - 会员等级 `Lv.3`，累计消费 `3098800` 分。
  - 钱包余额 `188800` 分，冻结金额 `26000` 分。
  - 订单 `5` 个，资产 `3` 台，托管收益 `296800` 分。
  - 一级团队 `2` 人，二级团队 `1` 人，已结算佣金 `89900` 分。
  - 报修 `3` 个，未读消息 `5` 条。
- 账务一致性：
  - 三笔已支付订单合计 `3098800` 分，与会员累计消费相等。
  - 钱包最后一笔流水余额 `188800` 分，与钱包账户余额相等。
  - 已结算托管收益 `268000` 分，与钱包托管入账合计相等。
  - 已结算佣金 `89900` 分，与钱包佣金入账合计相等。
  - 三笔已支付订单均有合同和 `PAID_ORDER` 会员事件。
- 关系完整性：
  - 资产来源订单、收益所属资产、佣金来源订单、报修所属设备、工单日志所属工单的孤儿记录均为 `0`。
  - 邀请关系无重复记录。
- `git diff --check` 通过。
- `npm run harness:ci`：`status=FAILURE`，`tests=5`，`passed=4`，`failed=1`。
  - `typeCheck=SUCCESS`。
  - 唯一失败项为既存前端测试 `pages.json registers all translated pages`，断言期望 9 个页面但当前仓库已有 10 个；与本次新增 SQL 和交付记录无关。

## 回退

按脚本中固定 ID，从子表到父表删除本次新增记录。目标用户仅在确认无其他业务记录后删除。
