# CI 结果

## 执行命令

| 命令 | 结果 | 说明 |
|---|---|---|
| `npm run type-check` | SUCCESS | `tsc --noEmit` 通过 |
| `npm run build:mp-weixin` | SUCCESS | UniApp 微信小程序构建通过，产物位于 `dist/build/mp-weixin` |
| `npm run test:unit` | SUCCESS | 3 个测试文件，26 个测试用例全部通过 |
| `npm run harness:ci` | SUCCESS | 已生成 `.harness/ci/latest.json` |

## Harness CI 程序化结果

```json
{
  "status": "SUCCESS",
  "tests": 26,
  "passed": 26,
  "total": 26,
  "failed": 0
}
```

## 门禁判定

- `status=SUCCESS`：满足。
- `tests>0`：26，满足。
- `passed=total`：26/26，满足。

## 备注

- 构建阶段输出 Sass legacy JS API deprecation warning，未导致构建失败。
- 额外检查 `dist/build/mp-weixin/custom-tab-bar` 已生成 `index.js/json/wxml/wxss`，避免微信端空白页配置缺失。
- `.harness/ci/latest.json` 已由 `npm run harness:ci` 写入。
