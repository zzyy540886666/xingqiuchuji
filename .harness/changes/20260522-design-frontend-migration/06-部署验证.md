# 部署验证

## 部署范围

- 本次未执行线上部署。
- 可验证构建产物：`dist/build/mp-weixin`。

## 参数确认

- API 基址仍优先使用 `VITE_API_BASE_URL`；未配置时小程序兜底为 `http://localhost:8080/api/v1`。
- 未新增敏感配置。
- 未修改支付、鉴权、订单幂等相关环境变量。

## 健康检查

- 本地构建：`npm run build:mp-weixin` 已通过。
- 程序化门禁：`npm run harness:ci` 已通过。
- 小程序结构检查：`dist/build/mp-weixin/custom-tab-bar/index.js/json/wxml/wxss` 已存在。
- 小程序人工验证建议：使用微信开发者工具导入 `dist/build/mp-weixin`，逐页检查首页、分类、搜索、商品详情、订单确认、社区、我的、会员中心。

## 回滚路径

- 若小程序视觉或交互验收失败，回滚本次涉及的前端页面文件与新增静态资源。
- 若真实后端联调失败，优先回退到 `src/services/types.ts` 与页面 mapper 对齐，不改变后端契约。
- 若本地后端端口不同，设置 `VITE_API_BASE_URL` 指向实际服务地址后重新构建。
- 若支付流程异常，保留幂等键逻辑，回退 `src/package-order/pages/order/create.vue` 到上一版分步确认流程。

## 未验证项

- 未进行微信开发者工具截图验收。
- 未进行真机支付链路验收。
