# CI结果

## 本地校验命令

- `npm run type-check`

## 结果

- `status=FAILED`
- `tests=0`
- `passed=0`

## 失败信息

- `vue-tsc` 在当前环境抛出异常：
  - `Search string not found: "/supportedTSExtensions = .*(?=;)/"`
  - Node: `v22.22.0`

## 回退路由

按流程规范：CI 失败且 `tests=0`，应回退到“单测编写/校验阶段”补齐可程序化测试与稳定校验环境。
