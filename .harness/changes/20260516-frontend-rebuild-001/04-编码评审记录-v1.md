# 04-编码评审记录-v1 — Claude 内部评审

> 阶段：Stage 5 | 日期：2026-05-16 | 角色：Reviewer (Claude) | 轮次：v1

---

## 评审结论

**判定：不通过（P0/P1 问题需在 v2 修复）**

---

## 7 维评审

### 1. 需求一致性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 1.1 | `src/pages/home/index.vue` — 首页 Tab 为空壳，仅有 AppTabBar。首页是用户第一入口，需 Banner/分类入口/推荐商品等基础内容 | 立即实现首页基础布局：顶部 Banner 轮播 + 场景分类入口 + 精选商品块。对接 `/catalog/skus` + `/config/app` | **P0** |
| 1.2 | `src/pages/mine/index.vue` — "我的" Tab 页仅有 "我的页 — 待开发" 占位文字。需求文档（FE-01）要求包含用户信息/会员徽章入口/设置/客服入口，用户确认需角色切换入口 | 立即实现"我的"页面：用户头像昵称区 + 菜单入口（会员/钱包/订单/资产/分销/报修/设置）+ 角色切换下拉选择 | **P0** |
| 1.3 | `src/pages/community/index.vue` — 社区 Tab 页仅有占位文字 | 社区 Tab 页应重定向到 `package-community/pages/community/index` 或展示简化 Feed | **P1** |
| 1.4 | `admin-console.vue` L94-95: 新建巡检计划 + 全部工单 仅为 toast 占位 | 实现创建巡检计划页面 + 全域工单查询页面 | P1 |
| 1.5 | 13 模块功能覆盖完整，FE-01~13 需求均有对应页面 | — | 通过 |

### 2. 架构一致性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 2.1 | 页面层无直接 `uni.request` 调用（grep 验证通过），所有 API 调用通过 `services/` 层 | — | 通过 |
| 2.2 | 组件层 (`components/`) 与页面层 (`pages/`) 分离清晰，公共组件 4 个已就位 | — | 通过 |
| 2.3 | Store 层按模块划分（session/config/catalog/orderDraft/cart），职责清晰 | — | 通过 |
| 2.4 | `services/types.ts` 集中管理所有 DTO 类型，340 行覆盖完整 | — | 通过 |

### 3. 契约一致性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 3.1 | API 路径均使用 `/api/v1` 前缀（在 http.ts 统一 baseURL），与接口通则一致 | — | 通过 |
| 3.2 | 金额字段使用整数分（grep 验证无 float/double），`formatMoney` 统一格式化 | — | 通过 |
| 3.3 | 响应信封 `{success, data, error, traceId}` 在 http.ts 中统一处理 | — | 通过 |
| 3.4 | 分页参数 `page/pageSize` 和 `cursor/limit` 与通则一致 | — | 通过 |
| 3.5 | openapi.yaml 当前为占位，接口字段以 API-0X.md 为参考。待后端 YAML 生成后需全局对齐检查 | 在 Stage 7 前做一次 grep 对比 | P2 |

### 4. 安全性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 4.1 | Token 持久化到 Storage + 401 自动清除，符合小程序安全基线 | — | 通过 |
| 4.2 | 用户输入（搜索/报修/发帖/提现）均有长度/类型限制 | — | 通过 |
| 4.3 | 提现 `Idempotency-Key` 已实现（`${Date.now()}-${Math.random()...}`） | — | 通过 |
| 4.4 | 社区内容审核状态由后端返回，前端不自行判定违规 | — | 通过 |
| 4.5 | 无 emoji 功能图标（grep 验证通过） | — | 通过 |

### 5. 数据一致性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 5.1 | 订单金额全程由后端试算接口返回 `previewResult.payableAmount`，前端仅展示 | — | 通过 |
| 5.2 | 操作按钮由后端 `allowedActions` 驱动（`order/list.vue`） | — | 通过 |
| 5.3 | 运营配置 (QA文案/话题/权益) 通过 `/config/app` 拉取，无硬编码 | — | 通过 |

### 6. 可观测性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 6.1 | 埋点 `trackPageView`/`trackClick` 已在关键页面和按钮集成 | — | 通过 |
| 6.2 | `track.ts` 实现批量/节流上报（5s 或 20 条阈值），失败不影响业务 | — | 通过 |
| 6.3 | `http.ts` 中 `catch { /* ignore */ }` 出现在 storage 读取失败和 mock 模式，不影响核心流程 | — | 通过 |
| 6.4 | 部分 catch 块仅 toast 提示，未记录 error 详情用于诊断 | — | P2 |

### 7. 可测试性

| # | 问题 | 建议 | 优先级 |
|---|------|------|--------|
| 7.1 | Mock 模式 (`MOCK_ENABLED=true`) 使所有 services 可脱离后端独立测试 | — | 通过 |
| 7.2 | `defineMock` 机制允许每个模块独立注入测试数据 | — | 通过 |

---

## 问题汇总

| 优先级 | 数量 | 说明 |
|--------|------|------|
| **P0** | 2 | 首页空壳 + 我的页空壳（用户第一入口不可为空） |
| P1 | 2 | 社区 Tab 占位 + 管理员快捷操作占位 |
| P2 | 2 | openapi.yaml 对齐 + error 日志完善 |

---

## 修复计划

P0: 立即实现首页 + 我的页
P1: 社区 Tab 重定向到分包 Feed + 管理员快捷操作
P2: 待 Stage 6 一并处理

---

_Codex 独立审查结果待后台 Agent 完成后合并到 v2_
