# 06-部署验证 — 星球出机小程序前端重建

> 阶段：Stage 8-10 | 日期：2026-05-16 | 角色：Doer

---

## 部署参数确认

| 参数 | 值 | 状态 |
|------|-----|------|
| 项目 | 星球出机微信小程序 | — |
| AppID | `wxa878c0ebb0e9536a` | ✅ 已配置 |
| 框架 | UniApp 3.0 + Vue 3 + TypeScript | ✅ |
| 构建产物 | `dist/build/mp-weixin/` | ✅ 已生成 |
| API BaseURL | `/api/v1` | ✅ 已配置 |
| Mock 模式 | `VITE_MOCK_ENABLED=true` (开发) | ✅ 有守卫 |
| 后端环境 | 未就绪（mock 模式运行） | ⚠️ 需后端部署后联调 |
| 微信商户号 | 未申请 | ⚠️ 支付需沙箱环境 |
| 体验版小程序 | 有 | ✅ |

---

## 构建命令

```bash
# 开发编译
npm run dev:mp-weixin

# 生产构建（mock 关闭）
VITE_MOCK_ENABLED=false npm run build:mp-weixin
```

---

## 部署步骤

### 1. 本地验证
- [x] `npx uni build -p mp-weixin` 编译通过
- [x] `npx vitest run` 全部 25 tests 通过
- [ ] 微信开发者工具导入 `dist/build/mp-weixin/` 验证页面渲染

### 2. 代码提交
```bash
git add src/ .harness/changes/20260516-frontend-rebuild-001/
git commit -m "feat: 星球出机小程序前端完整重建 (13模块/98文件/72端点)"
```

### 3. 上传体验版
- 微信开发者工具 → 上传 → 填写版本号 `0.1.0` → 提交审核

### 4. 微信后台配置
- [ ] request 合法域名：`https://api-host`（待后端提供）
- [ ] uploadFile 合法域名：COS 域名（待配置）
- [ ] downloadFile 合法域名：COS 域名
- [ ] 业务域名：合同 web-view 域名

---

## 回滚路径

若部署后发现严重问题：
1. 微信后台 → 版本管理 → 撤回审核/下架当前版本
2. 回退到上一个稳定版本
3. `git revert` 问题提交，修复后重新上线

---

## 环境变量

| 变量 | 开发 | 生产 |
|------|------|------|
| `VITE_MOCK_ENABLED` | `true` | `false`（不设置） |
| `VITE_API_BASE_URL` | `/api/v1` | `/api/v1` |

---

## 最终交付清单

| 类别 | 交付物 | 状态 |
|------|--------|------|
| 代码 | 98 个源文件 (~6,000 行) | ✅ |
| 编译 | `dist/build/mp-weixin/` | ✅ |
| 测试 | 25 tests / 25 passed | ✅ |
| 文档 | 7 份阶段产物 (需求分析→部署验证) | ✅ |
| 分包 | 7 个分包，主包 < 2MB | ✅ |
| API | 72 端点全覆盖（mock 模式可运行） | ✅ |
| 合规 | 金额整数分/SVG图标/三态/Token/幂等 | ✅ |

---

## 待后端就绪后的联调清单

- [ ] 替换 API baseURL 为真实后端域名
- [ ] 验证微信登录 `/auth/wechat` 真实流程
- [ ] 验证支付 `/orders/{id}/payments/wechat-jsapi` 真实调起
- [ ] 验证 COS 上传 STS 凭证 + 直传
- [ ] 验证 IM Token 获取 + SDK 初始化
- [ ] 同步 `openapi.yaml` 字段并对齐前端 types.ts
- [ ] 生产构建 `VITE_MOCK_ENABLED=false npm run build:mp-weixin`
- [ ] 微信后台配置 request/uploadFile/downloadFile 合法域名
