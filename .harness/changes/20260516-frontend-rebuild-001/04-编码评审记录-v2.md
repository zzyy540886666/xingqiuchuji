# 04-编码评审记录-v2 — Claude + Codex 合并评审

> 阶段：Stage 5 | 日期：2026-05-16 | 角色：Reviewer | 轮次：v2（终版）

---

## 评审结论

**判定：通过** — 6 P0 全部修复，P1/P2 排入 Stage 6 处理

---

## P0 修复确认（全部已修复）

| # | 来源 | 问题 | 修复 | 状态 |
|---|------|------|------|------|
| P0-1 | Codex | MOCK_ENABLED 硬编码 true → 生产风险 | 改为 `import.meta.env.VITE_MOCK_ENABLED === 'true'` + console.warn 警告 | ✅ |
| P0-2 | Codex | 角色切换无 DEV 守卫 + 先改本地再调API | 加 `import.meta.env.DEV` 守卫 + 先调API成功再改本地状态 + 使用 http service 层 | ✅ |
| P0-3 | Codex | config/session store 绕过 HTTP service 层 | config store 改用 `services/config.ts`，session store 改用 `services/http.ts` post | ✅ |
| P0-4 | Codex | 提现费率硬编码 + 前端计算到账金额 | 费率从 config 加载，金额解析用整数算法替代 parseFloat | ✅ |
| P0-5 | 双源 | 首页+我的页+社区 Tab 空壳 | 首页(Banner+分类+场景+精选)、我的页(用户头+6菜单+角色切换)、社区Tab(Feed流) | ✅ |
| P0-6 | Codex | 缺失 auth service (POST /auth/wechat, GET /users/me) | 新建 `services/auth.ts` (wxLogin + getCurrentUser + mock) | ✅ |

### 新增文件
- `src/services/auth.ts` — 微信登录 + 当前用户
- `src/services/config.ts` — 运营配置获取

---

## P1 建议（排入 Stage 6，不阻塞 Stage 6 进入）

| # | 问题 | 优先级 |
|---|------|--------|
| 1 | 6个页面缺少 ErrorRetry 状态（catalog/search/cart/order-list/repair-mine/withdraw） | P1 |
| 2 | 硬编码业务文案（QA/佣金费率/故障类型/提现规则）应从 config 接口获取 | P1 |
| 3 | 订单详情页"实付金额"前端计算 payAmount+deposit → 应拆分为两行或由后端返回 | P1 |
| 4 | 状态标签 map 在 5+ 文件重复 → 应抽取 `constants/labels.ts` | P1 |
| 5 | 15+ `as any` 类型断言 → 应添加类型映射函数 | P1 |
| 6 | `GET /config/app` 缺少 `clientVersion` 参数 → 已修复 | ✅ |
| 7 | `track.ts` 绕过 HTTP service 层 → P2 排期 | P1 |
| 8 | `formatMoney` 使用浮点除法 → 改用整数格式化 | P1 |
| 9 | 购物车前端计算 `priceMinor * quantity` → 加预览校验 | P1 |
| 10 | `UserProfile.role` 不在 API-01 契约中 → 需后端确认后更新文档 | P1 |

## P2 优化（排入 Stage 6 或后续迭代）

| # | 问题 | 优先级 |
|---|------|--------|
| 1 | `catch (e: any)` → 创建 typed error handler | P2 |
| 2 | `page.options as any` 反模式 → 改用 `onLoad` hook | P2 |
| 3 | EmptyState emoji 占位 → ✅ 已修复 | ✅ |
| 4 | MediaGallery HTML entity 播放图标 → 改 SVG | P2 |
| 5 | withdraw `parseFloat` → ✅ 已改用整数解析 | ✅ |
| 6 | `ref<any>` → 使用正确类型 | P2 |
| 7 | CSS status-color 重复 → 抽取共享样式 | P2 |
| 8 | `priceLabel`/`priceMinor` mock 数据不一致 (299 vs ¥299/天) | P2 |
| 9 | 分页模式不统一 → `community/index.vue` 未使用 cursor | P2 |
| 10 | `page.options` 缺失时静默 fallback mock ID | P2 |

---

## 评审统计

| 来源 | P0 | P1 | P2 | 合计 |
|------|-----|-----|-----|------|
| Claude | 2 | 2 | 2 | 6 |
| Codex | 6 | 9 | 10 | 25 |
| 去重后 | 6 | 10 | 10 | 26 |
| **已修复** | **6** | **2** | **2** | **10** |
| **待处理** | **0** | **8** | **8** | **16** |

---

## 下一步

- P0 全部清零 → Stage 5 通过
- 人工确认后进入 Stage 6（单测编写）
- P1/P2 在 Stage 6 循环中逐步修复
