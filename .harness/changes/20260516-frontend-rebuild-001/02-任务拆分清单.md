# 02-任务拆分清单 — 星球出机小程序前端重建

> 阶段：Stage 3 | 日期：2026-05-16 | 角色：Doer

---

## 拆分原则

- 每任务 = 最小可交付单元（可独立编译验证）
- 按 Phase 0→1→2→3→4 顺序执行，Phase 内部可并行
- 每个模块独立完成 编码→自检→提交 闭环

---

## Phase 0: M0 基础设施（Day 1-2）

### T-001: 工程骨架 — TypeScript + UniApp 配置
- **模块**: FE-01 (应用壳)
- **分层**: 配置层
- **文件**: `src/env.d.ts`, `src/manifest.json`
- **验证**: `npm run dev:mp-weixin` 无报错
- **依赖**: 无
- **风险**: 低
- **阻塞**: 无

### T-002: 路由与分包 — pages.json
- **模块**: FE-01 (应用壳)
- **分层**: Page 路由层
- **文件**: `src/pages.json`
- **内容**: 4 Tab 页 + 7 分包声明 + 全局样式配置
- **验证**: 微信开发者工具可识别全部路由
- **依赖**: T-001
- **风险**: 低
- **阻塞**: 无

### T-003: 设计 Token — SCSS 变量体系
- **模块**: FE-12 (公共组件)
- **分层**: 样式层
- **文件**: `src/styles/tokens.scss`
- **验证**: 编译通过，变量无未定义引用
- **依赖**: T-001
- **风险**: 低
- **阻塞**: 无

### T-004: 工具函数 — formatMoney / maskMobile / compactText
- **模块**: FE-12 (公共组件)
- **分层**: Mapper 层
- **文件**: `src/utils/format.ts`
- **验证**: formatMoney(12345) → "¥123.45"
- **依赖**: T-001
- **风险**: 低
- **阻塞**: 无

### T-005: 埋点工具 — track.ts
- **模块**: FE-12 (公共组件)
- **分层**: Adapter 层
- **文件**: `src/utils/track.ts`
- **内容**: `trackEvent(name, props)` + 批量/节流上报 → `POST /analytics/events`
- **验证**: 调用后控制台可见事件队列
- **依赖**: T-001, T-008 (http.ts)
- **风险**: 低
- **阻塞**: 依赖 T-008

### T-006: HTTP 客户端 — services/http.ts
- **模块**: FE-01 (应用壳)
- **分层**: API Client 层
- **文件**: `src/services/http.ts`
- **内容**:
  - baseURL `/api/v1`
  - Bearer Token 拦截器（从 storage 读取 sessionToken）
  - 401 → 清空 token + reject + 引导重新登录
  - 响应信封归一化 `{success, data, error, traceId}`
  - 支持 mock 数据注入（适配后端环境未就绪）
  - 超时 15s + 重试 1 次
- **验证**: 编译通过 + mock 请求返回预期数据
- **依赖**: T-001
- **风险**: 中（需支持 mock 模式）
- **阻塞**: 无（关键基础设施）

### T-007: 登录态管理 — stores/session.ts
- **模块**: FE-01 (应用壳)
- **分层**: Store 层
- **文件**: `src/stores/session.ts`
- **内容**:
  - State: `token`, `user` (id/nickname/avatarUrl/membershipLevel/role)
  - Actions: `restore()` (从 storage 加载), `login()` (wx.login → /auth/wechat), `switchRole()` (dev-only), `clear()`
  - Getter: `isLoggedIn`
- **验证**: restore 后 token 正确加载
- **依赖**: T-006
- **风险**: 低
- **阻塞**: 无

### T-008: 运营配置管理 — stores/config.ts
- **模块**: FE-01 (应用壳)
- **分层**: Store 层
- **文件**: `src/stores/config.ts`
- **内容**: `bootstrap()` → `GET /config/app`，缓存配置版本号，`onShow` 时 ETag 增量刷新
- **验证**: bootstrap 后配置可用
- **依赖**: T-006, T-007
- **风险**: 低
- **阻塞**: 无

### T-009: App.vue + main.ts 启动引导
- **模块**: FE-01 (应用壳)
- **分层**: Page 层
- **文件**: `src/App.vue`, `src/main.ts`
- **内容**: App.onLaunch → session.restore + config.bootstrap
- **验证**: 启动无报错
- **依赖**: T-007, T-008
- **风险**: 低
- **阻塞**: 无

### T-010: 公共组件 — AppTabBar / EmptyState / LoadingSkeleton / ErrorRetry
- **模块**: FE-12 (公共组件)
- **分层**: Component 层
- **文件**:
  - `src/components/AppTabBar.vue` — 4 Tab 底栏（SVG 图标）
  - `src/components/EmptyState.vue` — 空态（图标 + 标题 + 可选按钮）
  - `src/components/LoadingSkeleton.vue` — 骨架屏
  - `src/components/ErrorRetry.vue` — 错误重试（消息 + 重试按钮）
- **验证**: 各组件独立可渲染
- **依赖**: T-003
- **风险**: 低
- **阻塞**: 无

### T-011: SVG 图标集 — static/icons/
- **模块**: FE-12 (公共组件)
- **分层**: 静态资源
- **文件**: `src/static/icons/` (home/catalog/community/mine/cart/order/search/arrow/doc 等)
- **验证**: 图标在组件中正确渲染
- **依赖**: 无
- **风险**: 低
- **阻塞**: 无

---

## Phase 1: M1 核心交易链（Day 3-5）

### T-101: 商品货架 — 列表与筛选
- **模块**: FE-02
- **分层**: Page + Component + Service + Store
- **文件**: `src/pages/catalog/index.vue`, `src/components/ProductCard.vue`, `src/components/FilterDrawer.vue`, `src/services/catalog.ts`, `src/stores/catalog.ts`
- **API**: `GET /catalog/skus`, `GET /catalog/search`
- **验证**: 列表分页加载 + 筛选联动 + 搜索防抖 300ms
- **依赖**: Phase 0 完成
- **风险**: 中（搜索接口 filter 参数格式待确认）
- **阻塞**: 无

### T-102: 场景聚合页
- **模块**: FE-02
- **分层**: Page + Service
- **文件**: `src/pages/scene/detail.vue`, `src/services/catalog.ts`（追加）
- **API**: `GET /catalog/scenes/{sceneId}/bundles`
- **验证**: 场景卡片正确展示组合内容
- **依赖**: T-101
- **风险**: 低
- **阻塞**: 无

### T-103: 商品详情
- **模块**: FE-03
- **分层**: Page + Component + Service
- **文件**: `src/pages/product/detail.vue`, `src/components/MediaGallery.vue`, `src/components/PriceBlock.vue`, `src/components/StickyActionBar.vue`, `src/services/catalog.ts`（追加）
- **API**: `GET /catalog/skus/{skuId}`
- **验证**: 租/买/软件三种类型详情正确渲染
- **依赖**: T-101
- **风险**: 低
- **阻塞**: 无

### T-104: 下单流程 — 步骤机 + 试算
- **模块**: FE-04
- **分层**: Page + Store + Service
- **文件**: `src/pages/order/create.vue`, `src/stores/orderDraft.ts`, `src/services/orders.ts`
- **API**: `POST /orders/preview`, `POST /orders`
- **验证**: 步骤流转正确 + 试算结果展示 + 草稿持久化
- **依赖**: T-103
- **风险**: 高（资金链路核心，需 mock 支付参数）
- **阻塞**: 无

### T-105: 微信支付 + 结果页
- **模块**: FE-04
- **分层**: Page + Adapter
- **文件**: `src/pages/order/pay-result.vue`, `src/services/orders.ts`（追加）
- **API**: `POST /orders/{id}/payments/wechat-jsapi`, `GET /orders/{id}`
- **验证**: 支付调起 + 成功/失败/取消三态
- **依赖**: T-104
- **风险**: 高（微信商户号未就绪，以 mock 验证流程）
- **阻塞**: 微信商户号未申请

### T-106: 合同下载
- **模块**: FE-04
- **分层**: Page + Service
- **文件**: `src/pages/order/detail.vue`（合同入口块）
- **API**: `GET /orders/{id}/contract/download-url`
- **验证**: 下载链接获取 + web-view 预览
- **依赖**: T-105
- **风险**: 低

### T-107: 订单中心 — 列表与详情
- **模块**: FE-05
- **分层**: Page + Component + Service + Store
- **文件**: `src/pages/order/list.vue`, `src/pages/order/detail.vue`, `src/stores/order.ts`, `src/services/orders.ts`（追加）
- **API**: `GET /orders`, `GET /orders/{id}`
- **验证**: 三 Tab + 状态筛选 + 操作按钮由 allowedActions 驱动
- **依赖**: T-105
- **风险**: 低

### T-108: 购物车页
- **模块**: FE-04 辅助
- **分层**: Page + Store
- **文件**: `src/pages/cart/index.vue`, `src/stores/cart.ts`
- **验证**: 购物车增删改 + 去结算跳转
- **依赖**: T-101
- **风险**: 低

---

## Phase 2: M2 用户体系（Day 6-7）

### T-201: 会员等级与权益
- **模块**: FE-06
- **分层**: Page + Service + Store
- **文件**: `src/pages/member/index.vue`, `src/services/member.ts`, `src/stores/member.ts`
- **API**: `GET /users/me/membership`
- **验证**: 等级进度条 + 权益列表（配置驱动）
- **依赖**: Phase 1 完成
- **风险**: 低

### T-202: 星球卡购买
- **模块**: FE-06
- **分层**: Page + Service
- **文件**: `src/pages/member/planet-card.vue`, `src/services/member.ts`（追加）
- **API**: `POST /membership/planet-cards/{skuId}/purchase`
- **验证**: SKU 列表 → 支付 → 刷新等级
- **依赖**: T-201, T-105 (支付)
- **风险**: 中（支付链路复用）

### T-203: 钱包与光年币
- **模块**: FE-07
- **分层**: Page + Service + Store
- **文件**: `src/pages/wallet/index.vue`, `src/services/wallet.ts`, `src/stores/wallet.ts`
- **API**: `GET /wallet`, `GET /wallet/ledger`
- **验证**: 余额展示 + 流水分页
- **依赖**: Phase 1 完成
- **风险**: 低

### T-204: 提现
- **模块**: FE-07
- **分层**: Page + Service
- **文件**: `src/pages/wallet/withdraw.vue`, `src/services/wallet.ts`（追加）
- **API**: `POST /wallet/withdraw`（含 Idempotency-Key）
- **验证**: 金额校验 + 二次确认 + 防重复提交 + 提现状态展示
- **依赖**: T-203
- **风险**: 中（资金操作）

### T-205: 分销中心 — 首页/邀请/团队
- **模块**: FE-11
- **分层**: Page + Service + Store
- **文件**: `src/pages/distribution/home.vue`, `src/pages/distribution/invite.vue`, `src/pages/distribution/team.vue`, `src/services/distribution.ts`
- **API**: `GET /distribution/me`, `GET /distribution/team`
- **验证**: 邀请码展示 + 海报生成 + 团队列表
- **依赖**: Phase 1 完成
- **风险**: 中（海报生成需 Canvas 能力）

### T-206: 分销佣金
- **模块**: FE-11
- **分层**: Page + Service
- **文件**: `src/pages/distribution/commission.vue`, `src/services/distribution.ts`（追加）
- **API**: `GET /distribution/commissions`
- **验证**: 佣金分类 + 分页 + 提现联动
- **依赖**: T-205, T-204
- **风险**: 低

---

## Phase 3: M3 资产与社区（Day 8-10）

### T-301: 我的分身资产
- **模块**: FE-08
- **分层**: Page + Service
- **文件**: `src/pages/asset/list.vue`, `src/pages/asset/device.vue`, `src/services/asset.ts`
- **API**: `GET /assets/robots`, `GET /assets/robots/{assetId}`
- **验证**: 资产列表 + 设备详情
- **依赖**: Phase 2 完成
- **风险**: 低

### T-302: 托管上架 + 设备看板
- **模块**: FE-08
- **分层**: Page + Component + Service
- **文件**: `src/pages/asset/trusteeship.vue`, `src/services/asset.ts`（追加）
- **API**: `POST /assets/robots/{assetId}/trusteeship`, `GET /assets/robots/{assetId}/dashboard`
- **验证**: 时段选择 + 提交 + 看板指标
- **依赖**: T-301
- **风险**: 中（日历组件时区对齐）

### T-303: 社区 Feed 流
- **模块**: FE-09
- **分层**: Page + Component + Service + Store
- **文件**: `src/pages/community/index.vue`, `src/components/PostCard.vue`, `src/services/community.ts`, `src/stores/community.ts`
- **API**: `GET /community/feed`, `GET /community/posts/{postId}`
- **验证**: Feed 游标分页 + 话题筛选 + 帖子详情
- **依赖**: Phase 1 完成
- **风险**: 低

### T-304: 发帖 + COS 上传
- **模块**: FE-09
- **分层**: Page + Component + Adapter
- **文件**: `src/pages/community/editor.vue`, `src/utils/upload.ts`, `src/services/community.ts`（追加）
- **API**: `POST /community/upload/sts`, `POST /community/posts`
- **验证**: 编辑 → 上传 → 提交 → 审核状态展示
- **依赖**: T-303
- **风险**: 中（COS STS 凭证流程）

### T-305: 圈子
- **模块**: FE-09
- **分层**: Page + Service
- **文件**: `src/pages/community/circles.vue`, `src/services/community.ts`（追加）
- **API**: `GET /community/circles`, `POST /community/circles/{id}/join`, `GET /community/circles/{id}/feed`
- **验证**: 圈子列表 + 加入 + 圈内 Feed
- **依赖**: T-303
- **风险**: 低

### T-306: 即时通讯 — 会话与聊天
- **模块**: FE-10
- **分层**: Page + Adapter + 独立分包
- **文件**: `src/pages/im/conversations.vue`, `src/pages/im/chat.vue`, `src/services/im.ts`
- **API**: `GET /im/token` + 腾讯云 IM SDK
- **验证**: Token 获取 + 会话列表 + 消息收发
- **依赖**: Phase 2 完成
- **风险**: 高（腾讯云 IM SDK 集成复杂度、厂商文档变化）
- **阻塞**: IM SDK 具体版本选型

---

## Phase 4: M4 报修工单系统（Day 11-13）

### T-401: 报修 — 用户侧（提交/进度/列表/扫码）
- **模块**: FE-13 (USER)
- **分层**: Page + Service + Store
- **文件**: `src/pages/repair/create.vue`, `src/pages/repair/progress.vue`, `src/pages/repair/mine.vue`, `src/pages/common/qr-scan.vue`, `src/services/repair.ts`, `src/stores/repair.ts`
- **API**: `POST /work-orders`, `GET /work-orders`, `GET /work-orders/{id}`, `GET /work-orders/{id}/logs`, `POST /qrcodes/resolve`
- **验证**: 报修提交流程 + 进度时间线 + 扫码兜底
- **依赖**: Phase 0 完成（独立模块，不强依赖 M1-M3）
- **风险**: 中（图片上传 + 扫码能力）

### T-402: 工单 — 维修员侧（大厅/处理/巡检）
- **模块**: FE-13 (TECHNICIAN)
- **分层**: Page + Service + Store
- **文件**: `src/pages/repair/technician-workorders.vue`, `src/pages/repair/inspection-tasks.vue`, `src/services/repair.ts`（追加）, `src/stores/inspection.ts`
- **API**: `POST /work-orders/{id}/accept`, `POST /work-orders/{id}/reject`, `POST /work-orders/{id}/progress`, `POST /work-orders/{id}/complete`, `GET /inspection/tasks`, `POST /inspection/tasks/{id}/start`, `POST /inspection/tasks/{id}/submit`
- **验证**: 工单状态流转 + 巡检任务执行
- **依赖**: T-401
- **风险**: 中（角色权限隔离）

### T-403: 管理后台 — 管理员侧（计划/监控/管控/看板/通知）
- **模块**: FE-13 (ADMIN)
- **分层**: Page + Service + Store
- **文件**: `src/pages/repair/admin-console.vue`, `src/services/repair.ts`（追加）
- **API**: `POST /inspection/plans`, `GET /inspection/plans`, `PATCH /inspection/plans/{id}`, `GET /inspection/reports/summary`, `POST /work-orders/{id}/assign`, `GET /reports/*`, `GET /dashboard/*`, `GET /notifications`
- **验证**: 计划创建 + 指派 + 看板指标 + 通知列表
- **依赖**: T-402
- **风险**: 中（数据看板可视化）

### T-404: 反馈与知识库
- **模块**: FE-13 (公共)
- **分层**: Page + Service
- **文件**: `src/pages/common/feedback.vue`, `src/services/repair.ts`（追加）
- **API**: `POST /feedbacks`, `GET /feedbacks`, `GET /knowledge/articles`
- **验证**: 反馈提交 + 知识库浏览
- **依赖**: T-401
- **风险**: 低

---

## Phase 5: M5 联调测试（Day 14-15）

### T-501: 全模块接口联调
- **验证**: 全部 72 端点请求/响应格式与文档一致
- **依赖**: 后端环境就绪

### T-502: 分包编译验证
- **验证**: `npm run dev:mp-weixin` 主包+分包均 < 2MB
- **依赖**: Phase 0-4 完成

### T-503: 三态验证
- **验证**: 每个页面 loading/error/empty 覆盖
- **依赖**: Phase 0-4 完成

### T-504: 资金链路专项
- **验证**: 下单→试算→支付→回调 流程正确
- **依赖**: T-105

### T-505: 角色权限
- **验证**: USER/TECHNICIAN/ADMIN 数据隔离
- **依赖**: T-403

---

## 依赖关系图

```
T-001 ──→ T-002 ──→ T-003
  │         │
  └──→ T-006 ──→ T-007 ──→ T-008 ──→ T-009
          │         │
          └──→ T-005    └──→ T-010 (T-003)

T-010 (公共组件就绪)
  │
  ├──→ T-101/102 ──→ T-103 ──→ T-104 ──→ T-105 ──→ T-106
  │                                                │
  ├──→ T-108                                       └──→ T-107
  │
  ├──→ T-201 ──→ T-202
  ├──→ T-203 ──→ T-204
  ├──→ T-205 ──→ T-206
  │
  ├──→ T-301 ──→ T-302
  ├──→ T-303 ──→ T-304 ──→ T-305
  ├──→ T-306 (独立分包)
  │
  └──→ T-401 ──→ T-402 ──→ T-403
         └──→ T-404
```

---

## 风险汇总

| 任务 | 风险 | 缓解 |
|------|------|------|
| T-104/105 | 微信商户号未就绪 | mock 支付参数，流程验证优先 |
| T-205 | Canvas 海报生成兼容性 | 使用 uni.canvasToTempFilePath |
| T-302 | 日历时区对齐 | 统一 ISO 字符串传递 |
| T-304 | COS STS 凭证 | 使用后端签发的临时凭证 |
| T-306 | 腾讯云 IM SDK 集成 | 独立分包隔离，预留适配层 |
| T-401 | 微信扫码能力 | 扫码失败时提供手动输入兜底 |
