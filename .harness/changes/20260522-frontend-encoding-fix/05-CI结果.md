# CI 结果

## 仓库级门禁

- 命令：`npm.cmd run harness:ci`
- 结果：SUCCESS
- tests：26
- passed：26
- total：26
- failed：0
- 程序化结果文件：`.harness/ci/latest.json`

备注：直接执行 `npm run harness:ci` 时被 PowerShell 执行策略拦截 `npm.ps1`，随后使用 Windows 可执行入口 `npm.cmd` 执行同一命令成功。

## 构建验证

- 命令：`npm.cmd run build:mp-weixin`
- 结果：构建成功，输出 `dist/build/mp-weixin`
- 非阻断提示：Dart Sass `legacy-js-api` deprecation warning，来自依赖链路，不影响本次构建结果。

## 乱码扫描

- 命令：对 `index.html` 与 `src/**/*.vue|ts|json` 扫描常见 UTF-8 错解片段。
- 结果：未发现匹配。
- 命令：对 `dist/build/mp-weixin/**/*.wxml|json|js` 扫描常见 UTF-8 错解片段。
- 结果：未发现匹配。
