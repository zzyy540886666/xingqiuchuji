# 后台管理网站文档设计 - CI 结果

## 1. 执行信息

| 项 | 内容 |
| --- | --- |
| 日期 | 2026-05-24 |
| 命令 | `npm run harness:ci` |
| 程序化产物 | `.harness/ci/latest.json`、`.harness/ci/frontend-test-results.json` |
| `status` | `FAILURE` |
| `tests` | `5` |
| `passed` | `4` |
| `total` | `5` |

## 2. 门禁判定

| 条件 | 实际结果 | 判定 |
| --- | --- | --- |
| `status=SUCCESS` | `FAILURE` | 不满足 |
| `tests>0` | `5` | 满足 |
| `passed=total` | `4 != 5` | 不满足 |

结论：仓库级 CI 门禁未通过，不能声明最终质量门禁通过或进入真实部署执行。

## 3. 失败明细

| Gate/测试 | 失败原因 | 与本次文档改动关系 | 回退路由 |
| --- | --- | --- | --- |
| `typeCheck` / `frontend-static-check.mjs` | `frontend/src/pages.json` 缺少 `pages/order-confirm/index`、`pages/membership-center/index` 页面注册 | 本次仅新增/修改 `docs/` 与当前 changes 记录；该前端目录在任务启动检查时已为未跟踪工作区内容 | 按规则属于实现阶段问题，需由对应前端需求处理 |
| `pages.json registers all translated pages` | 期望 8 个页面注册，实际为 6 个 | 同上 | 按规则回退相关前端实现阶段 |

## 4. 文档专项验证

| 检查 | 结果 |
| --- | --- |
| `docs/README.md` 与 `docs/admin/*.md` 相对 Markdown 链接存在性检查 | 通过，验证 4 个文件 |
| 本次新增/变更 Markdown 文件尾随空白检查 | 通过，最终复查 11 个文件 |

## 5. 风险与后续处理

- 本轮文档已经产出并经过文档专项复核，但仓库全局交付门禁保持失败状态。
- 不在本需求内修改处于既有变更中的小程序页面注册；修复该前端问题后应重新执行 `npm run harness:ci` 并新增 CI 记录版本或更新本记录。
