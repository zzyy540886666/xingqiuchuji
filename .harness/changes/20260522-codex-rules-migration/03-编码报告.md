# 03-编码报告

## 改动范围

1. 新增 `.harness/rules/04-trae规则迁移-codex常驻规则.md`。
2. 更新 `AGENTS.md`，将 Rule 04 纳入 Codex 常驻加载清单。
3. 新增本需求 Changes 留痕目录与文档。

## 规则对齐

- 对齐 `.trae/rules/harness-enforcement.md`：迁移流程门禁、产物链、CI 回退、Wiki 查阅协议。
- 对齐 `.trae/rules/karpathy-guidelines.md`：迁移先澄清、最小改动、可验证成功标准。
- 对齐 `.trae/rules/xingqiu-dev-principles.md`：迁移安全、契约、工程化、UI 图标约束。
- 对齐 `.trae/rules/xingqiu-stack.md`：迁移技术栈、基础设施、文档真源约束。

## 接口影响

无接口变更。

## 数据影响

无数据结构或迁移影响。

## 风险与未验证项

1. 本次只迁移规则，不改变 Trae 原规则文件。
2. 规则遵循依赖 Codex 读取 `AGENTS.md` 和 `.harness/rules/04-*`；已通过入口索引强化。
3. CI 结果需在 `05-CI结果.md` 中记录。

## 验证计划

1. 检查新增规则文件存在。
2. 检查 `AGENTS.md` 包含 Rule 04。
3. 执行 `npm run harness:ci` 并记录结果。
