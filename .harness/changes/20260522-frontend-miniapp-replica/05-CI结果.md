# 05-CI结果

## 命令

```bash
npm run harness:ci
```

## 程序化结果

```json
{
  "status": "SUCCESS",
  "tests": 5,
  "passed": 5,
  "total": 5,
  "failed": 0
}
```

## 结果文件

- `.harness/ci/latest.json`
- `.harness/ci/frontend-test-results.json`

## 覆盖项

- `pages.json` 注册 8 个翻译页面。
- 金额格式化工具校验整数分。
- 请求封装包含 `/api/v1`、`Authorization`、`Bearer`、`Idempotency-Key`。
- 翻译页面保留核心文案：星球·出机、场景应用、为你推荐、分类、社区、会员权益、星球卡等。
- 商品详情与订单确认保留租赁购买流程核心标签。

## 未执行项

- 无。已执行 `frontend` 的真实 UniApp 构建。

## 构建结果

```bash
cd frontend
npm run build:mp-weixin
```

结果：`DONE Build complete.`

微信开发者工具可导入：

```text
frontend\dist\build\mp-weixin
```

开发产物也已刷新：

```text
frontend\dist\dev\mp-weixin
```

## 针对开发者工具报错的复核

- `frontend/dist/dev/mp-weixin/app.wxss` 已移除 `* { ... }`。
- `frontend/dist/build/mp-weixin/app.wxss` 已移除 `* { ... }`。
- `frontend/dist/dev/mp-weixin` 与 `frontend/dist/build/mp-weixin` 中已无 `https://images.unsplash.com` 外链。
