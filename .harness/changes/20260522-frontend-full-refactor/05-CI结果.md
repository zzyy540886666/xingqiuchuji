# CI 结果

## 命令

```bash
npm.cmd run harness:ci
```

## 结果

- status: `SUCCESS`
- tests: `33`
- passed: `33`
- total: `33`
- failed: `0`

## 门禁

- typeCheck: `SUCCESS`
- unitTests: `SUCCESS`
- nonZeroTests: `SUCCESS`
- allPassed: `SUCCESS`

## 产物

- `.harness/ci/latest.json`
- `.harness/ci/vitest-results.json`

## 额外构建验证

```bash
npm.cmd run build:mp-weixin
```

结果：成功，产物目录 `dist/build/mp-weixin`。

提示：构建日志存在 Sass legacy JS API deprecation warning 与 `os - Alias not found`，但退出码为 0。
