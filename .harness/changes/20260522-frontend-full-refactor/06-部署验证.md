# 部署验证

## 本轮状态

本需求未执行真实部署；已完成本地小程序构建验证。

## 构建参数

```bash
npm.cmd run build:mp-weixin
```

- 平台：`mp-weixin`
- 产物目录：`dist/build/mp-weixin`
- 构建结果：成功

## 手工验收入口

- 使用微信开发者工具导入 `dist/build/mp-weixin`。
- 首屏检查：首页、分类、社区、我的 tabbar。
- 主链路检查：搜索、商品详情、订单确认、会员中心。
- 接口检查：确保运行环境 API Base URL 指向后端 `/api/v1`。

## 回滚路径

- 若页面复刻偏差影响验收：回退对应 `src/pages/**` 或分包页面改动。
- 若后端联调失败：优先回退 `src/services/http.ts`、`src/services/catalog.ts`、`src/services/orders.ts` 与相关类型改动。
- 若 CI 再次出现路径问题：保留 `tools/harness/ci-gate.mjs` 的真实路径与 `.cmd` 调整。
