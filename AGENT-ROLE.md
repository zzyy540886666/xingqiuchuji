# 受控交付治理

## 1. 适用范围

本文件仅在 `AGENTS.md` 判定进入受控交付，或用户明确要求正式治理/审计材料时读取。普通实现任务遵循 `AGENTS.md` 直接工作，不因修改代码而自动进入本流程。

## 2. 目标

受控交付用于处理高风险或需要正式证据的任务。它的目标是把重要决策、验证与回退写清楚，而不是把一次实现拆成大量形式文件或交接分支。

## 3. 触发条件

- API 破坏性变更、Schema/数据库迁移、兼容性切换。
- 鉴权、安全、敏感数据、金额、订单、支付或结算逻辑。
- 生产外部集成、部署发布、回滚或不可逆操作。
- 用户要求正式方案、审计、独立评审、验收或发布材料。
- 影响面较大且缺少集中记录会明显增加错误风险的跨模块改动。

## 4. 核心约束

1. 接口字段、枚举和状态码受影响时，以 `docs/api/openapi/openapi.yaml` 为契约真源并同步受影响调用方。
2. 金额使用整数分；支付、订单和结算关键状态变化可审计且关键写入具备幂等处理。
3. 触及输入、存储或外部集成时，不拼接 SQL、不吞异常、不提交敏感配置，并落实必要鉴权、超时与失败处理。
4. 涉及后端分层时遵循 Controller / Service / Domain / DAO / Adapter 边界。
5. 验证结果、未验证事项与回退方式必须真实明确。

## 5. 最小执行路径

1. 明确目标、边界、主要风险和成功标准。
2. 读取与风险直接相关的 Rule、Skill、契约或领域文档。
3. 实施最小必要改动并运行与风险匹配的验证。
4. 更新一个核心交付记录；只有发生独立评审或部署时才追加专项记录。

不要求预先生成阶段文档，不要求执行者/评判者分离，除非用户要求独立判定或发布治理确有需要。

## 6. 正式产物

核心文件：`.harness/changes/<需求ID>/delivery.md`

`delivery.md` 至少包含：

- 目标与范围
- 风险触点和关键决策
- 实际改动
- 验证证据与结果
- 未验证事项、回退或后续动作

可选文件：

- `review-vN.md`：仅用于已执行的独立正式评审，历史版本不删除。
- `deployment.md`：仅用于实际部署/发布，记录参数、健康检查和回滚路径。

## 7. 门禁

高风险运行时代码或部署交付前执行：

```bash
npm run harness:ci
```

成功声明必须记录 `status=SUCCESS`、`tests>0`、`passed=total`。门禁失败时针对根因修复或回退，不新增无助于解决问题的流程分支。

## 8. 按需索引

- 工程结构与分层：`.harness/rules/01-工程结构-模块职责-五层架构.md`
- 受控交付规则：`.harness/rules/02-开发流程规范-阶段与门禁.md`
- 工程硬性规范：`.harness/rules/03-工程硬性规范.md`
- 风险主题参考：`.harness/rules/04-trae规则迁移-codex常驻规则.md`
- 编码、评审、测试 SOP：`.harness/skills/`
- 业务知识与契约：`.harness/wiki/`、`docs/`
