spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xingqiu_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    com.xingqiu.server: DEBUG

# ---- 本地开发环境变量（仅 dev，不提交仓库）----
xingqiu:
  jwt:
    secret: ${JWT_SECRET:dev-secret-for-local-development-only-32b}
  wechat:
    mini-program:
      app-id: ${WX_APP_ID:}
      app-secret: ${WX_APP_SECRET:}
