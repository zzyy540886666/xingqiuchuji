# Codex 默认入口

## 1. 默认原则

Codex 以完成用户目标为默认路径。收到任务后先读取实现所需的最少上下文，随后直接调查、修改并验证；不要仅因修改了代码或配置，就先创建流程文档、评审轮次、交接记录或工作分支。

普通小项目工作默认不加载 `AGENT-ROLE.md`、全部 `.harness/rules/*` 或 `.harness/skills/*`。只在任务触及相应约束时读取相关文件，例如改接口时读取 OpenAPI，改资金或鉴权逻辑时读取安全约束。

## 2. 两种模式

| 模式 | 触发条件 | 工作方式 | 产物 |
| --- | --- | --- | --- |
| 默认执行 | 咨询、文档、UI、小功能、缺陷修复、局部重构、一般代码/配置/测试调整 | 直接实施；读取目标文件和必要契约；运行与改动匹配的验证 | 仅最终回复中的变更和验证摘要 |
| 受控交付 | 用户明确要求正式方案/审计/评审/证据链/上线交付；或涉及 API 破坏性变更、数据库迁移、鉴权与安全、金额/订单/支付、外部生产集成、部署发布、不可逆操作 | 读取 `AGENT-ROLE.md` 和相关 Rule/Skill；记录关键决策和验证证据 | 一个核心 `delivery.md`，仅按实际需要追加 `review-vN.md` 或 `deployment.md` |

任务范围扩大或实施中识别出高风险点时，从默认执行升级到受控交付即可；不要求重跑已完成且仍有效的调查和验证。

## 3. 默认执行要求

1. 能从仓库确定实现方式时直接开始，不先产出需求分析或任务拆分文档。
2. 只读取与当前目标、影响模块和验证命令有关的内容；同一会话内未变化的文件不重复读取。
3. 修改运行时代码时运行最聚焦的可行测试、类型检查或构建；无法运行时明确说明原因和风险。
4. 不为普通任务创建 `.harness/changes/` 目录、独立评审文件、部署文件或 git 分支。
5. 最终回复聚焦已完成内容、验证结果和仍存在的真实风险。

## 4. 受控交付要求

1. 在 `.harness/changes/<YYYYMMDD-短名>/delivery.md` 中集中记录目标、范围、关键决策、改动、验证、风险与回退。
2. 仅在执行独立正式评审时添加 `review-vN.md`；仅在部署/发布时添加 `deployment.md`。
3. 高风险运行时代码或部署交付前运行 `npm run harness:ci`，并在 `delivery.md` 记录可核验结果。
4. 只有用户明确要求、并行隔离确有必要或发布流程规定时，才创建/切换分支。

## 5. 始终适用的底线

1. 不覆盖无关用户改动，不伪造验证结果，不把“看起来没问题”作为证据。
2. 影响接口时以 `docs/api/openapi/openapi.yaml` 为真源；金额使用整数分；涉及输入、SQL、鉴权、幂等或外部调用时遵循对应安全约束。
3. 只为实际存在的风险、决策和验证生成记录，不为流程完整性制造衍生产物。

## 6. 星球出机开发约束

1. 所有外部输入默认不可信：请求参数、路径 id、header、URL 参数和小程序本地数据都必须按类型、长度、范围、枚举和未知字段策略校验。
2. 禁止字符串拼接 SQL；查询、排序和筛选必须使用参数化查询、ORM 绑定参数或严格白名单映射。
3. 展示用户生成内容时默认防 XSS：不信任前端传入 HTML；需要富文本时使用白名单标签与属性；小程序端不得把未消毒内容当 HTML 解析。
4. 做业务和接口改动时按攻击者视角检查：鉴权绕过、资源越权、刷接口、金额或订单状态篡改、重复提交和重放；发现威胁必须补服务端控制，例如资源级权限、幂等、限流、审计和服务端金额计算。
5. 后端部署在自有服务器；前后端路径、字段名、枚举状态必须与 `docs/api/`、`docs/api/openapi/openapi.yaml` 和实际实现一致。改接口时同步后端 Controller/DTO、OpenAPI、小程序 `services/` 调用方和必要文档。
6. 新增代码按功能分文件、分模块，避免巨型单文件；模块边界优先参考 `docs/frontend/`、`docs/backend/` 和现有代码结构。
7. 优先复用已有服务、组件、工具函数、DTO、schema 和校验逻辑；不要复制粘贴式新增平行实现，也不要为未出现的场景提前抽象。
8. 异常处理必须面向生产：修根因，保留可观测日志或 traceId，避免吞异常、只打补丁或向客户端泄露堆栈、SQL、密钥和内部路径。
9. UI 功能图标禁止使用 emoji；使用项目已有图标资源或极简 SVG，并保持小程序端展示一致。
10. 触及安全、接口契约、后端实现、缓存一致性或交付审查时，按需读取并遵循 `.cursor/skills/xingqiu-secure-by-default`、`xingqiu-api-contract`、`xingqiu-springboot-backend`、`xingqiu-data-cache-consistency`、`xingqiu-code-review-delivery`。

## 7. 未跟踪文件保护

1. 编辑、替换、移动或删除目标文件前，必须先确认目标文件是否被 Git 跟踪：优先使用 `git ls-files -- <path>`；同时用 `git status --short -- <path>` 识别未跟踪文件。
2. 对未跟踪文件不得采用“先删除再重建”的流程。需要整文件替换时，先读取完整内容并在同目录创建临时备份，或使用不会删除原文件的补丁式更新；确认新文件写入成功后再清理自己创建的备份。
3. 撤销改动时必须区分已跟踪和未跟踪文件：已跟踪文件可从 Git 恢复；未跟踪文件不能假设可由 Git 恢复。若必须修改未跟踪文件，先建立可回滚副本，并在最终汇报中说明。
4. 删除未跟踪文件、生成文件或临时文件前，必须确认它是本次任务中自己创建的，或得到用户明确要求；否则不要删除。
