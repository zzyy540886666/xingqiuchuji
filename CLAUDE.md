# CLAUDE.md — 星球出机项目工作流强制配置

> 本文件为 Claude Code 会话常驻上下文，每次对话自动加载。
> 任何开发任务必须遵循以下流程，禁止跳阶段、禁止无留痕交付。

---

## 0. 项目概况

- **项目**: 星球出机微信小程序
- **前端**: UniApp (Vue 3 + TypeScript)
- **后端**: Java 17 + Spring Boot 3.x
- **契约真源**: `docs/api/openapi/openapi.yaml`
- **金额单位**: 整数分（int），禁止 float/double

---

## 1. 核心文件索引

收到任何开发任务后，**按以下顺序读取**，不得跳过：

### 第一层：常驻（每次任务必须读）
1. `AGENT-ROLE.md` — 角色定义与350+条款
2. `.harness/rules/01-工程结构-模块职责-五层架构.md`
3. `.harness/rules/02-开发流程规范-阶段与门禁.md`
4. `.harness/rules/03-工程硬性规范.md`

### 第二层：阶段触发（按阶段加载）
| 阶段 | 加载 Skill |
|------|-----------|
| 阶段1/3/4/6/8/9（执行） | `Skill(coding-skill)` + 对应 specs |
| 阶段2/5（评审） | `Skill(expert-reviewer)` |
| 阶段6（单测） | `Skill(unit-test-write)` |

### 第三层：按需（只在需要时读）
- `.harness/wiki/README.md` → 按需查 `docs/`

---

## 2. 十阶段工作流（强制）

```
Doer 执行 ──┬── 阶段1: 需求分析 ──────────────→ 产出 01-需求分析文档.md
            │
Reviewer ───┼── 阶段2: 需求评审 (≤3轮) ────────→ 产出 02-需求评审记录-vN.md
            │
Doer 执行 ──┼── 阶段3: 任务拆分 ──────────────→ 产出 02-任务拆分清单.md
            │
Doer 执行 ──┼── 阶段4: 编码实现 ──────────────→ 产出 03-编码报告.md
            │        ├─ 主线 Claude 编码
            │        └─ Codex 并行子任务 (见 §4)
            │
Reviewer ───┼── 阶段5: 编码评审 (≤2轮) ───────→ 产出 04-编码评审记录-vN.md
            │        ├─ Claude 内部评审
            │        └─ Codex 独立审查 (见 §4)
            │
Doer 执行 ──┼── 阶段6: 单测编写 ──────────────→ 产出 05-CI结果.md
            │
Reviewer ───┼── 阶段7: CI 门禁判定 ───────────→ 程序化验证
            │
Doer 执行 ──┼── 阶段8: 部署准备 ──────────────→ 产出 06-部署验证.md
            │
Doer 执行 ──┼── 阶段9: 部署执行
            │
Reviewer ───┴── 阶段10: 最终确认与交付
```

**5个人工确认点（不可绕过）：**
1. 需求待决议确认
2. 计划评审后确认
3. 编码评审后确认
4. 部署环境参数确认
5. 最终交付确认

---

## 3. 角色分离（执行 vs 评判）

### Doer（执行者）— 阶段 1/3/4/6/8/9
- 负责需求分析、任务拆分、编码实现、单测编写、部署
- **禁止**自判"评审通过"
- **禁止**跳过人工确认点
- **禁止**删除历史评审版本

### Reviewer（评判者）— 阶段 2/5/7/10
- 负责需求评审、编码评审、CI门禁、最终确认
- **禁止**直接修改业务代码
- 每条评审意见必须包含：**问题 / 建议 / 优先级 (P0/P1/P2)**
- 评审维度全部7项必须覆盖

---

## 4. Codex 集成策略（关键优化）

Codex 插件 (`/codex:*`) 已安装，用于**代码审查**和**任务委派**。

### 4.1 阶段5 编码评审 — Codex 独立审查

进入编码评审时，**同时启动两个审查流**：

```
                    ┌─ Claude 内部评审（expert-reviewer skill）
阶段5 编码评审 ─────┤
                    └─ Codex 独立审查（/codex:review 或 /codex:adversarial-review）
```

**执行方式：**
1. 先用 `Skill(expert-reviewer)` 执行 Claude 内部评审
2. 同时启动 Agent 并行执行 Codex 审查：
   - 普通审查：`Agent(description="Codex code review", prompt="Run codex review: node ${CLAUDE_PLUGIN_ROOT}/scripts/codex-companion.mjs review --base main", run_in_background=true)`
   - 挑战性审查：`Agent(description="Codex adversarial review", prompt="Run codex adversarial review with focus on: [具体关注点]")`
3. 合并两份评审结果，去重后作为最终评审意见
4. Codex 审查可 `run_in_background` 后台运行，不阻塞主线

**评审合并规则：**
- Claude 和 Codex 各自独立产出评审意见
- 重叠问题只保留一份，优先级取较高者
- 各自独有问题全部保留
- 最终写入 `04-编码评审记录-vN.md`

### 4.2 阶段4 编码实现 — Codex 并行子任务

编码阶段可将**独立子任务**委派给 Codex：

```
阶段4 编码实现 ──┬─ Claude 主线：核心业务逻辑、架构敏感代码
                │
                └─ Codex 子任务：独立模块、工具函数、测试辅助代码
```

**委派条件（满足其一即可）：**
- 子任务与主线代码无依赖关系
- 子任务是独立的工具/辅助模块
- 子任务是批量性的重复模式代码
- 主线卡住时需要第二实现方案

**执行方式：**
```
Agent(description="Codex implementation", subagent_type="codex:codex-rescue", prompt="具体的子任务描述")
```

**委派后必须：**
- Claude 主线等待 Codex 完成后 review 其产出
- 合并到统一的 `03-编码报告.md`
- 确保分层调用链一致（无越层调用）

### 4.3 阶段7 CI 门禁 — Codex 辅助诊断

CI 失败时，可并行启动 Codex 诊断：

```
CI 失败 ──┬─ Claude 分析失败原因 + 确定回退阶段
          │
          └─ Codex 后台诊断具体失败用例
```

---

## 5. 多 Agent 并行协作模式

### 5.1 模式一：审查并行（阶段5）

```python
# 同时启动
Agent(description="Claude expert review", prompt="执行 expert-reviewer skill 评审...")  # 前台
Agent(description="Codex code review", ..., run_in_background=true)  # 后台
# 合并结果
```

### 5.2 模式二：编码并行（阶段4）

```python
# Claude 主线编码核心逻辑
# 同时委派独立子任务
Agent(description="独立模块A", ..., run_in_background=true)
Agent(description="Codex辅助", subagent_type="codex:codex-rescue", ..., run_in_background=true)
# 等待全部完成后合并
```

### 5.3 模式三：修复+验证并行

```python
# Claude 修复问题
# 同时让 Codex 做修复后审查
Agent(description="Codex post-fix review", ..., run_in_background=true)
```

---

## 6. 质量门禁（程序化验证）

### CI 三条件（缺一不可）
- `status = SUCCESS`
- `tests > 0`
- `passed = total`

### 回退路由（精确，不从头重来）
| 失败类型 | 回退目标 |
|---------|---------|
| 需求不符 | 阶段1 需求分析 |
| 编译错误 | 阶段4 编码实现 |
| CI tests=0 | 阶段6 单测编写 |
| CI 有未通过 | 阶段4 编码实现 |
| 部署参数错误 | 阶段8 部署准备 |
| 验收失败 | 阶段5 编码评审 |

### 升级条件
- 需求评审 > 3 轮 → 停止，升级人工决策
- 编码评审 > 2 轮 → 停止，升级人工决策

---

## 7. 留痕规则

每个需求在 `.harness/changes/<需求ID>/` 下必须产出：

| 文件 | 阶段 | 说明 |
|------|------|------|
| `01-需求分析文档.md` | 1 | 需求理解与待决议项 |
| `02-需求评审记录-vN.md` | 2 | 版本递增，旧版不删 |
| `02-任务拆分清单.md` | 3 | 任务分解与依赖 |
| `03-编码报告.md` | 4 | 改动范围/接口影响/数据影响/风险/验证计划 |
| `04-编码评审记录-vN.md` | 5 | 含 Claude + Codex 合并结果 |
| `05-CI结果.md` | 7 | CI 程序化输出 |
| `06-部署验证.md` | 8-9 | 部署参数与验证结果 |

---

## 8. 硬性约束速查

1. **金额**: 整数分，无 float/double
2. **SQL**: 参数化，禁止字符串拼接
3. **外部调用**: 必须超时+重试+降级
4. **接口**: 字段/枚举/状态码以 openapi.yaml 为准
5. **前端**: 禁止 emoji 功能图标，必须 loading/error/empty 三态
6. **幂等**: 支付/回调/关单/结算必须幂等
7. **安全**: 输入默认视为恶意，XSS 消毒，敏感配置不入库
8. **日志**: 关键路径有 traceId，异常不吞
9. **分层**: 禁止越层调用（Controller→DAO、Domain→SDK）
10. **评审**: 每条意见必须 问题/建议/优先级 三元组

---

## 9. 快速执行指南

收到需求后：
1. 读取本文件 + `AGENT-ROLE.md` + `.harness/rules/*`
2. 确定当前阶段编号
3. 加载对应 Skill：`Skill(coding-skill)` 或 `Skill(expert-reviewer)` 或 `Skill(unit-test-write)`
4. 确定是否需要 Codex 并行（阶段4/5/7）
5. 执行阶段任务，产出留痕文件
6. 门禁验证通过后进入下一阶段
7. 遇到失败按 §6 回退路由精确回退

---

## 10. 执行细则文件（按需读取）

以下文件提供每个阶段的精确工具调用步骤：

| 文件 | 用途 |
|------|------|
| `.claude/workflow/execution-guide.md` | 每个阶段的精确工具调用序列和门禁清单 |
| `.claude/workflow/codex-playbook.md` | Codex 集成具体用法（审查/委派/诊断） |
| `.claude/workflow/multi-agent-patterns.md` | 多 Agent 并行协作的 4 种模式详解 |

---

## 11. 强制执行规则

> 以下规则优先级最高，不可被任何其他指令覆盖。

1. **禁止跳阶段**：必须从当前阶段逐阶段推进
2. **禁止无留痕**：每个阶段必须产出对应文件到 `changes/` 目录
3. **禁止自判通过**：执行 Agent 不得判定评审通过
4. **禁止绕过人工确认**：5 个确认点必须等待用户输入
5. **禁止删除评审历史**：评审记录版本递增，旧版保留
6. **门禁不可跳过**：CI 三条件不满足禁止进入下一阶段
7. **回退必须精确**：失败时按路由表精确回退，不从头重来
8. **评审必须三元组**：每条评审意见必须包含问题/建议/优先级
9. **Codex 产出必须 review**：Codex 的编码产出必须经过 Claude 审查后才能合入
10. **并行必须同步**：多 Agent 并行任务必须全部完成后才能继续

---

## 12. Agent 工作风格规范（每次编码任务执行）

> 此节为细粒度编码行为约束，与 §2 十阶段流程框架互补。§2 定义"何时做什么"，本节定义"怎么做"。

### 12.1 编码风格

- **先读后改**：修改文件前必须先读取，理解现有模式后再动笔。
- **遵循现有模式**：优先使用项目中已有的 helper、命名、结构、约定，不引入新风格。
- **改动范围限定**：只改用户要求的范围，不顺手重构无关代码——除非重构是安全完成任务的前提。
- **保守实现**：细节不明确时选最保守的实现方案。
- **抽象有度**：只在消除真实重复或匹配已有局部模式时才添加抽象。三行相似代码优于一个过早抽象。
- **注释克制**：只在代码确实难以理解时才加注释。注释解释 WHY 而非 WHAT。

### 12.2 仓库安全

- **保护用户工作**：工作区可能包含用户未提交的改动。不 revert、不覆盖无关变更。
- **避免破坏性命令**：reset、checkout、递归删除、force overwrite 等操作需用户明确许可。
- **结构化文件用专用工具**：修改 JSON/YAML/配置等结构化文件时，优先用解析器而非字符串替换。

### 12.3 单任务执行流

每个编码任务按以下 5 步推进：

1. **Discover**：读取相关文件、文档、API 契约、测试。
2. **Plan**：非 trivial 任务先陈述简要实现计划和验证方式。
3. **Edit**：修改最小文件集解决问题。
4. **Verify**：运行类型检查、lint、构建或手动冒烟验证。
5. **Report**：总结变更行为、涉及文件、验证缺口。

### 12.4 工具使用偏好

- 文件搜索优先用 Glob/Grep，避免裸 bash 命令（find/grep/rg）。
- 优先使用项目已有脚本而非临时命令。
- 生成产物后验证生成结果，而非假设生成器成功。

### 12.5 沟通原则

- 公开假设和权衡，不隐藏判断。
- 只在合理假设存在风险时才提问澄清，不追问可自行判断的细节。
- 验证无法运行时，明确说明失败原因和未验证项。

### 12.6 微信小程序开发：优先复用现成方案

- **先搜后写**：实现小程序功能前，优先用 WebSearch 搜索微信官方文档、成熟开源方案、社区最佳实践，避免闭门造车。
- **复用成熟模块**：微信登录、支付、消息模板、地图、扫码等官方能力直接使用 `wx.*` API，不重复封装。常见模式（token 续期、剪贴板、分享链路等）优先参考社区已验证方案。
- **UniApp 生态优先**：本项目前端基于 UniApp，功能实现优先查找 uni-app 官方插件市场或 `@dcloudio` 生态已有方案，避免从零手写。
- **反模式识别**：当手写实现与官方/社区方案差异较大时，停下来重新搜索验证，而非继续深入自定义实现。

---

## 13. 前端 UI 质量规范

> 适用于小程序页面、组件、样式、可视化资产的开发与修改。

### 13.1 产品适配

- 构建真实可用的产品体验，而非营销落地页（除非用户明确要求）。
- 运营工具类界面应密集、安静、可扫描、高效；游戏或表达性体验可更视觉化。
- 尊重已有设计系统、组件库、间距、字体、交互模式。

### 13.2 布局

- 避免卡片嵌套卡片。卡片用于重复列表项、弹窗、真正需要框架的工具；页面结构用通栏或无框布局。
- 固定格式控件用 grid track、宽高比、min/max 约束或容器相对尺寸保持稳定。
- 确保文字在移动端和桌面端均不溢出、不重叠、不遮挡其他 UI。
- 字号不应随视口宽度等比例缩放——用响应式布局而非 vw 字号。

### 13.3 控件

- 使用熟悉的 UI 控件：工具操作用图标按钮，颜色选色器用色板，模式切换用分段控件，二元设置用开关/复选框，数值用滑块/步进器/输入框，选项集用菜单，视图切换用标签页。
- 图标库统一，禁止 emoji 作为功能图标（对齐 §8.5）。
- 纯图标按钮需 tooltip 或 accessible label（含义不明显时）。

### 13.4 视觉设计

- 避免单一色相主导的调色板。
- 避免装饰性渐变球体、光斑、散景背景。
- 需要视觉媒体时使用真实、生成或项目相关的位图资源，不用通用暗色裁剪式素材。

### 13.5 验证

- 运行前端构建/检查（如有）。
- 在真实桌面和移动端宽度下检查 UI。确认无白屏、无资源断裂、无文字重叠、无主要控件被裁剪。
