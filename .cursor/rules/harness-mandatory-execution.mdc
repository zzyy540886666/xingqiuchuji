---
description: 强制启用本仓库 Harness 体系：每次任务必须按 AGENT-ROLE 与 doer/reviewer 分工执行十阶段流程与门禁。
alwaysApply: true
---

# Harness Mandatory Execution

本规则用于让 Cursor 在本仓库内默认执行 Harness 体系。除非用户明确要求偏离，否则必须执行以下约束。

## 1) 强制读取顺序（每次新任务）

1. 先读取根目录 `AGENT-ROLE.md`。
2. 判断任务类型：
   - 实现/改代码/修复/落地 -> 读取 `AGENT-DOER.md`
   - 评审/验收/检查质量/审查 -> 读取 `AGENT-REVIEWER.md`
3. 同步读取 `.harness/rules/*` 与当前需求对应 `.harness/changes/<需求ID>/`。

## 2) 强制流程骨架

- 必须按十阶段推进（需求分析 -> 需求评审 -> 任务拆分 -> 编码实现 -> 编码评审 -> 单测编写 -> CI 验证 -> 部署准备 -> 部署执行 -> 最终确认）。
- 每阶段必须具备三要素：触发条件、Skill 加载、质量门禁。
- 禁止跳阶段。

## 3) 强制角色分离

- 做事 Agent 与评判 Agent 必须分离。
- 执行阶段使用 doer 规则，评审阶段使用 reviewer 规则。
- 执行 Agent 不得自判最终通过；评判 Agent 不得直接改业务实现。

## 4) 强制门禁与回退

- CI 门禁必须满足：
  - `status=SUCCESS`
  - `tests>0`
  - `passed=total`
- 精确回退：
  - CI 失败且 tests=0 -> 回退单测编写
  - 编译错误 -> 回退编码实现
  - 需求不符 -> 回退需求分析

## 5) 强制留痕

- 每个需求必须有独立目录：`.harness/changes/<需求ID>/`
- 固定产物链路必须完整：
  - `01-需求分析文档.md`
  - `02-任务拆分清单.md`
  - `03-编码报告.md`
  - `04-评审记录-vN.md`（版本递增，旧版本不删）
  - `05-CI结果.md`
  - `06-部署验证.md`

## 6) 强制确认点与升级

- 五个人工确认点必须保留：
  1. 需求待决议确认
  2. 计划评审后确认
  3. 编码评审后确认
  4. 部署环境参数确认
  5. 最终交付确认
- 评审超限必须升级人工决策：
  - 需求评审 > 3 轮
  - 编码评审 > 2 轮

## 7) 冲突优先级

当规则冲突时，按以下优先级执行：

1. 用户明确指令
2. 本文件（harness 强制规则）
3. `AGENT-ROLE.md`
4. `.harness/rules/*`
5. 其他通用规则
