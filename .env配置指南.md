# 🪐 星球出机小程序 — 正式部署上线配置指南

> **适用场景**：从零开始，把整个项目部署到一台云服务器上，面向公网提供服务。
> **目标读者**：小白也能操作，每一步都解释"这是干嘛的"和"去哪拿这个值"。

---

## 📋 目录

- [0. 正式部署你需要准备什么](#0-正式部署你需要准备什么)
- [1. 配置全景图：23 个参数一览](#1-配置全景图23-个参数一览)
- [2. 第一组：基础设施（数据库 / Redis / JWT）](#2-第一组基础设施数据库--redis--jwt)
- [3. 第二组：微信小程序（🔴 必填）](#3-第二组微信小程序-必填)
- [4. 第三组：微信支付（🟡 有支付功能才要）](#4-第三组微信支付-有支付功能才要)
- [5. 第四组：腾讯云 COS 对象存储（🟡 有图片上传才要）](#5-第四组腾讯云-cos-对象存储-有图片上传才要)
- [6. 第五组：腾讯云 TMS 文本内容安全（🟡 有社区发帖才要）](#6-第五组腾讯云-tms-文本内容安全-有社区发帖才要)
- [7. 第六组：运行模式（生产环境固定不改）](#7-第六组运行模式生产环境固定不改)
- [8. 部署流程（本地打包 → 上传服务器 → 启动）](#8-部署流程本地打包--上传服务器--启动)
- [9. 上线前 10 项检查清单](#9-上线前-10-项检查清单)
- [10. 常见问题与排查](#10-常见问题与排查)

---

## 0. 正式部署你需要准备什么

### 硬件 / 账号清单

| 序号 | 你需要的东西 | 去哪弄 | 费用参考 |
|:---:|---|------|:---:|
| 1 | 一台云服务器（2核4G 起） | 腾讯云 / 阿里云 | ¥50–100/月 |
| 2 | 一个域名（备案过的） | 腾讯云 / 阿里云 | ¥30–60/年 |
| 3 | 微信小程序账号 | [mp.weixin.qq.com](https://mp.weixin.qq.com) | 免费（个人） / ¥300认证（企业） |
| 4 | 小程序已通过审核上线 | 微信公众平台提交审核 | — |
| 5 | 微信支付商户号（可选） | [pay.weixin.qq.com](https://pay.weixin.qq.com) | 企业才能申请 |
| 6 | 腾讯云 COS 存储桶（可选） | [console.cloud.tencent.com](https://console.cloud.tencent.com) | 按量计费 |
| 7 | 服务器上装好 Docker | `curl -fsSL https://get.docker.com \| sh` | 免费 |

### 软件清单

| 你需要装的（本地电脑） | 用途 |
|---|---|
| [Docker Desktop](https://www.docker.com/products/docker-desktop/) | 本地构建镜像 |
| [Node.js 20+](https://nodejs.org/) | 编译 admin-web 前端 |
| [Maven 3.9+](https://maven.apache.org/) | 编译 Java 后端（或直接用 Docker 里的 Maven） |
| OpenSSH 客户端 | Windows 自带 `ssh` / `scp` |

---

## 1. 配置全景图：23 个参数一览

> 📌 `.env` 文件在项目根目录：`d:\Pro\星球出机小程序 - 副本\.env`

生产部署用到的 23 个参数，按**必须程度**分 6 组：

| 组 | 参数名 | 含义 | 必须程度 |
|:---:|---|------|:---:|
| 1️⃣ 基础设施 | `DB_USERNAME` | MySQL 账号 | 🔴 必填 |
| 1️⃣ | `DB_PASSWORD` | MySQL 密码 | 🔴 必填 |
| 1️⃣ | `DB_URL` | MySQL 连接地址 | 🔴 必填 |
| 1️⃣ | `REDIS_PASSWORD` | Redis 密码 | 🔴 必填 |
| 1️⃣ | `JWT_SECRET` | 用户登录令牌签名密钥 | 🔴 必填 |
| 2️⃣ 微信小程序 | `WX_APP_ID` | 小程序 AppID | 🔴 必填 |
| 2️⃣ | `WX_APP_SECRET` | 小程序密钥 | 🔴 必填 |
| 3️⃣ 微信支付 | `WX_PAY_MCH_ID` | 商户号 | 🟡 支付才要 |
| 3️⃣ | `WX_PAY_MERCHANT_SERIAL_NO` | API 证书序列号 | 🟡 支付才要 |
| 3️⃣ | `WX_PAY_API_V3_KEY` | APIv3 密钥 | 🟡 支付才要 |
| 3️⃣ | `WX_PAY_PRIVATE_KEY_PATH` | 商户私钥文件路径 | 🟡 支付才要 |
| 3️⃣ | `WX_PAY_PLATFORM_PUBLIC_KEY_PATH` | 平台公钥文件路径 | 🟡 支付才要 |
| 3️⃣ | `WX_PAY_NOTIFY_URL` | 支付回调通知地址 | 🟡 支付才要 |
| 4️⃣ COS 存储 | `COS_SECRET_ID` | 腾讯云 API 密钥 ID | 🟡 图片上传才要 |
| 4️⃣ | `COS_SECRET_KEY` | 腾讯云 API 密钥 Key | 🟡 图片上传才要 |
| 4️⃣ | `COS_REGION` | COS 存储桶地域 | 🟡 图片上传才要 |
| 4️⃣ | `COS_BUCKET` | COS 存储桶名称 | 🟡 图片上传才要 |
| 5️⃣ TMS 内容安全 | `TMS_SECRET_ID` | 腾讯云 API 密钥 ID | 🟡 社区发帖才要 |
| 5️⃣ | `TMS_SECRET_KEY` | 腾讯云 API 密钥 Key | 🟡 社区发帖才要 |
| 5️⃣ | `TMS_REGION` | TMS 服务地域 | 🟡 社区发帖才要 |
| 6️⃣ 运行模式 | `SPRINGDOC_ENABLED` | API 文档开关 | ✅ 固定值 |
| 6️⃣ | `SPRING_PROFILES_ACTIVE` | Spring 运行模式 | ✅ 固定值 |

---

## 2. 第一组：基础设施（数据库 / Redis / JWT）

### 2.1 `DB_USERNAME` — MySQL 账号

```
去哪拿：你自己设的。部署时 Docker Compose 会创建 MySQL 容器，这个账号是你指定的。

默认值：root（一般不改）
怎么填：DB_USERNAME=root
```

### 2.2 `DB_PASSWORD` — MySQL 密码

```
去哪拿：你自己设的。生产环境务必换掉默认值！

默认值：xingqiu123
⚠️ 生产建议：换成强密码（至少 16 位，含大小写+数字+符号）

怎么填：DB_PASSWORD=你的强密码
```

### 2.3 `DB_URL` — MySQL 连接地址

```
去哪拿：不需要去平台拿。这是 Docker 内部网络地址，固定写法。

⚠️ 注意：里面的 mysql 不是占位符！mysql 是 Docker 容器名，不能改。

默认值：jdbc:mysql://mysql:3306/xingqiu?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai

怎么填：原样保留，不需要改。
```

### 2.4 `REDIS_PASSWORD` — Redis 密码

```
去哪拿：你自己设的。生产环境务必换掉默认值！

默认值：redis123
⚠️ 生产建议：换成强密码

怎么填：REDIS_PASSWORD=你的强密码
```

### 2.5 `JWT_SECRET` — 登录令牌签名密钥

```
去哪拿：不需要去平台拿，你自己生成一组随机字符串。

⚠️ 极度重要！这是所有用户登录 Session 的签名密钥。
   泄露 = 任何人可以伪造任意用户身份登录。
   dev 环境有默认值，生产环境绝不能复用！

怎么生成（三选一）：
  方式1: 在线随机生成 → 打开 https://www.random.org/strings/ 生成 64 位
  方式2: 终端命令   → powershell -Command "[Convert]::ToBase64String((1..48|%{Get-Random -Max 256}))"
  方式3: 任意在线工具生成一串 64 位以上的随机字符串

怎么填：JWT_SECRET=此处贴你生成的64位随机串
```

---

## 3. 第二组：微信小程序（🔴 必填）

> 这两个参数是整个小程序能不能跑起来的核心。没有它们，用户无法登录。

### 3.1 `WX_APP_ID` — 小程序 AppID

```
去哪拿：微信公众平台 (mp.weixin.qq.com)

步骤：
  1. 浏览器打开 https://mp.weixin.qq.com/
  2. 微信扫码登录（用绑定小程序的微信）
  3. 左侧菜单：开发 → 开发管理
  4. 点击「开发设置」标签
  5. 找到「AppID(小程序ID)」，像这样：
     wx 9 5 6 b d f 2 4 1 c 1 5 e 9 4 1
     └──────────────────────────────────┘
     这就是你要的值

怎么填：WX_APP_ID=wx开头的那个字符串
```

### 3.2 `WX_APP_SECRET` — 小程序密钥

```
去哪拿：微信公众平台，跟 AppID 在同一个页面（开发者设置）

步骤：
  1. 同上进入「开发 → 开发管理 → 开发设置」
  2. 找到「AppSecret(小程序密钥)」
  3. 如果之前没保存（显示 ******），点击旁边的「重置」按钮
  4. 管理员微信扫码确认
  5. 系统会显示新的 AppSecret ← 立即复制！！
     ⚠️ 这个值只显示一次！刷新页面就没了！

怎么填：WX_APP_SECRET=刚复制的32位字符
```

---

## 4. 第三组：微信支付（🟡 有支付功能才要）

> ⚠️ 如果你暂时不上支付功能，这 7 个参数全部留空即可，不影响小程序其他功能。

> 📌 前置条件：小程序已完成微信认证（企业主体，¥300/年），且已开通微信支付。

### 4.1 `WX_PAY_MCH_ID` — 商户号

```
去哪拿：微信支付商户平台

步骤：
  1. 打开 https://pay.weixin.qq.com/
  2. 微信扫码登录
  3. 顶部「账户中心」→「商户信息」
  4. 找到「商户号」一栏，通常是 10 位数字
     像这样：1234567890

怎么填：WX_PAY_MCH_ID=你的10位商户号
```

### 4.2 `WX_PAY_MERCHANT_SERIAL_NO` — API 证书序列号

```
去哪拿：微信支付商户平台

步骤：
  1. 微信支付商户平台
  2. 左侧菜单：API 安全 → API 证书
  3. 找到「商户 API 证书」→ 点击证书序列号
     像这样：XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

怎么填：WX_PAY_MERCHANT_SERIAL_NO=证书序列号
```

### 4.3 `WX_PAY_API_V3_KEY` — APIv3 密钥

```
去哪拿：你自己生成，然后在商户平台设置

步骤：
  生成密钥（三选一）：
  方式1: 在线生成 → https://www.random.org/strings/ 生成 32 位字符串
  方式2: 命令行   → powershell -Command "[Convert]::ToBase64String((1..24|%{Get-Random -Max 256}))"
  方式3: 任意密码生成器生成 32 位随机串

  然后去商户平台设置：
  1. 微信支付商户平台
  2. 左侧菜单：API 安全 → APIv3 密钥
  3. 点击「设置」
  4. 把刚生成的 32 位密钥粘贴进去
  5. 确认

怎么填：WX_PAY_API_V3_KEY=你生成的32位密钥
```

### 4.4 `WX_PAY_PRIVATE_KEY_PATH` — 商户私钥文件路径

```
去哪拿：微信支付商户平台下载证书后得到的 apiclient_key.pem 文件

步骤：
  1. 微信支付商户平台 → API 安全 → API 证书
  2. 点击「下载证书」→ 得到一个压缩包
  3. 解压后找到 apiclient_key.pem
  4. 把这个文件放到项目 certs/ 目录下

  Docker 容器里挂载路径固定为 /app/certs/

怎么填：WX_PAY_PRIVATE_KEY_PATH=/app/certs/apiclient_key.pem
       （保持默认值即可，不需要改）
```

### 4.5 `WX_PAY_PLATFORM_PUBLIC_KEY_PATH` — 微信平台公钥文件路径

```
去哪拿：微信支付商户平台 → API 安全 → 平台证书

步骤：
  1. 商户平台 → API 安全 → 平台证书
  2. 下载「平台证书(公钥)」→ platform_public_key.pem
  3. 放到项目 certs/ 目录下

怎么填：WX_PAY_PLATFORM_PUBLIC_KEY_PATH=/app/certs/platform_public_key.pem
       （保持默认值即可）
```

### 4.6 `WX_PAY_NOTIFY_URL` — 支付回调通知地址

```
去哪拿：这是你的服务器公网地址 + 固定路径

⚠️ 关键约束：
  - 必须 HTTPS（微信支付强制要求）
  - 必须是公网可访问的域名
  - 必须是 80 或 443 端口（不能带端口号）
  - 不能用 IP 地址


怎么填：WX_PAY_NOTIFY_URL=https://你的域名/api/v1/internal/pay/wechat/notify

示例：  如果你的域名是 xingqiu.com
        WX_PAY_NOTIFY_URL=https://xingqiu.com/api/v1/internal/pay/wechat/notify
```
---

## 5. 第四组：腾讯云 COS 对象存储（🟡 有图片上传才要）

> ⚠️ 如果你暂时不需要图片上传功能（比如管理员不传 Banner 图），这组可以全留空。
> 📌 前置条件：注册腾讯云账号并完成实名认证。

### 5.0 用主账号还是子账号？（强烈建议子账号）

```
⚠️ 安全建议：不要用主账号（根账号）的密钥直接填进 .env。

  主账号密钥一旦泄露 = 对方可以操作你腾讯云上所有资源。
  应该创建一个子账号，只给它 COS 相关的最小权限。

创建子账号步骤（腾讯云控制台）：
  1. 打开 https://console.cloud.tencent.com/cam
  2. 左侧：「用户」→「用户列表」→「新建用户」→「自定义创建」
  3. 选择「可访问资源并接收消息」
  4. 填写用户名（如 xingqiu-cos）
  5. 勾选「编程访问」→ 勾选「自定义密码」或跳过密码
  6. 「从策略列表中选取策略关联」→ 搜索并勾选以下权限策略：
     ① QcloudCOSFullAccess           → COS 全部操作（读写、列举、删除）
     ② QcloudCOSDataFullControl      → COS 数据读写
  7. 点击「完成」→ 系统会显示 SecretId 和 SecretKey
     ⚠️ 这个 SecretKey 只显示一次！立即复制保存！
  8. 把 SecretId 和 SecretKey 填入 .env：
       COS_SECRET_ID=AKIDxxxxxxxx
       COS_SECRET_KEY=xxxxxxxx

子账号至少需要的 COS 权限（如果不用预设策略，可手动创建自定义策略）：
  ┌─────────────────────────────────────────────────┐
  │ 权限               │ 为什么需要                  │
  ├─────────────────────────────────────────────────┤
  │ cos:PutObject       │ 上传图片到 COS 存储桶       │
  │ cos:GetObject       │ 读取/下载 COS 中的文件      │
  │ cos:DeleteObject    │ 删除 COS 中的文件            │
  │ cos:HeadObject      │ 检查文件是否存在             │
  │ cos:GetBucket       │ 列举存储桶（STS 临时密钥用） │
  │ sts:GetFederationToken │ 生成临时上传凭证给小程序  │
  └─────────────────────────────────────────────────┘

  如果你不需要 STS 临时凭证上传（只用后端直传），sts:GetFederationToken 可以去掉。
```

### 5.1 `COS_SECRET_ID` — 腾讯云 API 密钥 ID

```
去哪拿：腾讯云控制台 → 访问管理

步骤：
  1. 打开 https://console.cloud.tencent.com/
  2. 微信/手机扫码登录
  3. 右上角头像 → 访问管理 → 访问密钥 → API 密钥管理
  4. 如果还没有密钥，点击「新建密钥」
  5. 你会看到：
     ┌──────────────────────────────────────┐
     │ SecretId:  AKIDxxxxxxxxxxxxxxxxxxxxx │ ← 这就是 COS_SECRET_ID
     │ SecretKey: xxxxxxxxxxxxxxxxxxxxxxxxx │ ← 这就是 COS_SECRET_KEY
     └──────────────────────────────────────┘

  ⚠️ 如果用子账号（推荐），密钥在创建子账号最后一步显示，不是在主账号 API 密钥页面。

怎么填：
  COS_SECRET_ID=AKID开头的那一串
  COS_SECRET_KEY=对应的SecretKey
```

### 5.2 `COS_SECRET_KEY` — API 密钥 Key

```
去哪拿：同上，跟 SecretId 在同一页拿到的 SecretKey

怎么填：COS_SECRET_KEY=你的 SecretKey
```

### 5.3 `COS_REGION` — 存储桶地域

```
去哪拿：你自己选的。去 COS 控制台创建存储桶时选择地域。

常用地域：
  ap-guangzhou --> 广州（华南，默认推荐）
  ap-shanghai   --> 上海（华东）
  ap-beijing    --> 北京（华北）
  ap-chengdu    --> 成都（西南）

怎么填：COS_REGION=ap-guangzhou
       （如果你在广州或华南地区，保持默认即可）
```

### 5.4 `COS_BUCKET` — 存储桶名称

```
去哪拿：腾讯云 COS 控制台 → 创建存储桶

步骤：
  1. 打开 https://console.cloud.tencent.com/cos/bucket
  2. 点击「创建存储桶」
  3. 填写桶名称（如 xingqiu-images-1234567890）
     ⚠️ 名字后面要加 APPID 数字后缀（系统会提示）
  4. 选择地域、访问权限（选「公有读私有写」）
  5. 创建完成后，桶名称就在列表里

怎么填：COS_BUCKET=存储桶完整名称（含 APPID 后缀）
```

---

## 6. 第五组：腾讯云 TMS 文本内容安全（🟡 有社区发帖才要）

> ⚠️ 如果你暂时不启用社区发帖功能（帖子内容审核），这组可以全留空。
> 📌 前置条件：注册腾讯云账号并完成实名认证。

### 6.0 TMS 是什么？不是腾讯地图！

```
⚠️ 重要澄清：
  这套配置里的 TMS 不是「腾讯地图（Tencent Map Service）」，
  而是腾讯云「文本内容安全（Text Moderation Service）」。

  它的作用是：用户在小程序社区发帖时，自动审核帖子文字内容是否违规
  （色情、暴力、政治敏感等），审核结果分三档：
    PASS        → 通过，帖子直接发布
    BLOCK       → 违规，帖子被拒绝
    MANUAL_REVIEW → 疑似，转人工审核

  如果审核接口调用失败，会自动降级为 MANUAL_REVIEW（人工审核），
  不会阻塞用户发帖功能。
```

### 6.1 用子账号创建 TMS 密钥（强烈建议）

```
⚠️ 安全建议：不要用主账号密钥。应该创建一个独立的子账号，只给 TMS 相关权限。

  你可以和 COS 共用一个子账号（给同一个子账号同时加 COS + TMS 权限），
  也可以单独创建一个 TMS 子账号。两个方案都可以，看你的管理习惯。

子账号至少需要的 TMS 权限（自定义策略）：
  ┌────────────────────────────────────────────┐
  │ 权限                   │ 为什么需要         │
  ├────────────────────────────────────────────┤
  │ cms:TextModeration     │ 调用文本内容安全审核 │
  └────────────────────────────────────────────┘

创建子账号步骤（和 COS 类似）：
  1. 打开 https://console.cloud.tencent.com/cam
  2. 「用户」→「用户列表」→「新建用户」→「自定义创建」
  3. 选择「可访问资源并接收消息」
  4. 填写用户名（如 xingqiu-tms）
  5. 勾选「编程访问」
  6. 「从策略列表中选取策略关联」→ 搜索并勾选：
     ① QcloudCMSFullAccess → 内容安全全部操作
        （如果你觉得 FullAccess 权限太大，可以手动创建自定义策略，
          只保留 cms:TextModeration 这一个权限）
  7. 点击「完成」→ 系统显示 SecretId 和 SecretKey
     ⚠️ 这个 SecretKey 只显示一次！立即复制保存！

如果 TMS 和 COS 共用同一个子账号：
  - 在步骤 6 同时勾选 COS 和 CMS 相关的策略即可。
  - .env 里 TMS_SECRET_ID 和 COS_SECRET_ID 可以填同一个值。
```

### 6.2 `TMS_SECRET_ID` — 腾讯云 API 密钥 ID

```
去哪拿：腾讯云控制台 → 访问管理 → API 密钥管理
        （或创建子账号时最后一步显示的 SecretId）

怎么填：TMS_SECRET_ID=AKID开头的那一串
       （如果你 COS 和 TMS 共用一个子账号，这里填和 COS_SECRET_ID 一样的值）
```

### 6.3 `TMS_SECRET_KEY` — 腾讯云 API 密钥 Key

```
去哪拿：同上，跟 SecretId 一起显示的 SecretKey

怎么填：TMS_SECRET_KEY=你的 SecretKey
       （如果和 COS 共用子账号，填和 COS_SECRET_KEY 一样的值）
```

### 6.4 `TMS_REGION` — 内容安全服务地域

```
去哪拿：不需要去平台拿，这是腾讯云 API 调用的地域参数。

常用地域：
  ap-guangzhou --> 广州（默认推荐）
  ap-shanghai   --> 上海
  ap-beijing    --> 北京

怎么填：TMS_REGION=ap-guangzhou
       （保持默认即可，一般不需要改）
```

---

## 7. 第六组：运行模式（生产环境固定不改）

```
SPRINGDOC_ENABLED=false
  → 生产环境关闭 API 文档（安全原则：不对外暴露接口文档）

SPRING_PROFILES_ACTIVE=prod
  → 启用生产配置（关闭 Debug 日志、关闭 Swagger）
```

> ⚠️ 生产环境不要改成 `dev` 或 `true`，否则有安全和性能风险。

---

## 8. 部署流程（本地打包 → 上传服务器 → 启动）

### 8.1 第一步：确认 .env 填写完毕

打开项目根目录的 `.env`，对照上面的说明逐项确认：
- [ ] 微信 AppID 和 AppSecret 已填真实值 ✅
- [ ] JWT_SECRET 已换成生产强随机值 ✅
- [ ] DB_PASSWORD 和 REDIS_PASSWORD 已换成强密码 ✅
- [ ] 其他参数按需填写或留空 ✅

### 8.2 第二步：在本地准备部署文件

打开 PowerShell，逐条执行：

```powershell
# 进入项目目录
Set-Location 'D:\Pro\星球出机小程序 - 副本'

# 创建部署用目录（如果还没有）
New-Item -ItemType Directory -Force -Path .\deploy\certs | Out-Null

# 1. 编译 admin-web 前端
Set-Location 'D:\Pro\星球出机小程序 - 副本\admin-web'
npm ci --registry=https://registry.npmmirror.com
npm run build
docker build -t xingqiu-admin-web:prod .

# 2. 编译后端（如果你本地装了 Maven，用这个）
Set-Location 'D:\Pro\星球出机小程序 - 副本\backend'
mvn -DskipTests clean package
docker build -t xingqiu-backend:prod .

# 如果你本地没装 Maven，用这个替代上面的 mvn ...：
# docker run --rm -v "${PWD}:/workspace" -w /workspace maven:3.9.9-eclipse-temurin-17 mvn -DskipTests clean package

# 3. 导出镜像为 tar 文件
Set-Location 'D:\Pro\星球出机小程序 - 副本'
docker save -o .\deploy\admin-web-prod.tar xingqiu-admin-web:prod
docker save -o .\deploy\backend-prod.tar xingqiu-backend:prod

# 4. 如果有微信支付证书，复制到 certs 目录（没有就跳过）
# Copy-Item .\certs\* .\deploy\certs\ -Force
```

### 8.3 第三步：在服务器上创建 docker-compose.prod.yml

这个文件决定了服务器上怎么跑你的容器。在项目根目录创建：

```powershell
Set-Location 'D:\Pro\星球出机小程序 - 副本'

@'
services:
  mysql:
    image: mysql:8.0
    container_name: xq-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: xingqiu
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --character-set-client-handshake=FALSE --init-connect='SET NAMES utf8mb4' --default-time-zone=+08:00
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: xq-redis
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    image: xingqiu-backend:prod
    container_name: xq-backend
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      WX_APP_ID: ${WX_APP_ID}
      WX_APP_SECRET: ${WX_APP_SECRET}
      WX_PAY_MCH_ID: ${WX_PAY_MCH_ID}
      WX_PAY_MERCHANT_SERIAL_NO: ${WX_PAY_MERCHANT_SERIAL_NO}
      WX_PAY_API_V3_KEY: ${WX_PAY_API_V3_KEY}
      WX_PAY_PRIVATE_KEY_PATH: ${WX_PAY_PRIVATE_KEY_PATH}
      WX_PAY_PLATFORM_PUBLIC_KEY_PATH: ${WX_PAY_PLATFORM_PUBLIC_KEY_PATH}
      WX_PAY_NOTIFY_URL: ${WX_PAY_NOTIFY_URL}
      COS_SECRET_ID: ${COS_SECRET_ID}
      COS_SECRET_KEY: ${COS_SECRET_KEY}
      COS_REGION: ${COS_REGION}
      COS_BUCKET: ${COS_BUCKET}
      TMS_SECRET_ID: ${TMS_SECRET_ID}
      TMS_SECRET_KEY: ${TMS_SECRET_KEY}
      TMS_REGION: ${TMS_REGION}
      SPRINGDOC_ENABLED: ${SPRINGDOC_ENABLED}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
    ports:
      - "127.0.0.1:8328:8080"
    volumes:
      - ./certs:/app/certs:ro
    networks:
      - internal
    restart: unless-stopped

  admin-web:
    image: xingqiu-admin-web:prod
    container_name: xq-admin-web
    ports:
      - "127.0.0.1:5137:80"
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  internal: {}

volumes:
  mysql_data:
  redis_data:
'@ | Set-Content -Encoding utf8 .\docker-compose.prod.yml
```

### 8.4 第四步：上传到服务器

```powershell
# 替换成你的服务器 IP
$SERVER_IP = "159.75.2.56"

# 在服务器上创建目录
ssh ubuntu@$SERVER_IP "mkdir -p /opt/xingqiu/deploy /opt/xingqiu/certs /opt/xingqiu/sql"

# 上传文件
scp .\.env ubuntu@${SERVER_IP}:/opt/xingqiu/.env
scp .\docker-compose.prod.yml ubuntu@${SERVER_IP}:/opt/xingqiu/
scp .\backend\sql\init.sql ubuntu@${SERVER_IP}:/opt/xingqiu/sql/
scp .\deploy\admin-web-prod.tar ubuntu@${SERVER_IP}:/opt/xingqiu/deploy/
scp .\deploy\backend-prod.tar ubuntu@${SERVER_IP}:/opt/xingqiu/deploy/

# 如果有微信支付证书（没有就跳过）
# scp .\certs\* ubuntu@${SERVER_IP}:/opt/xingqiu/certs/
```

### 8.5 第五步：在服务器上加载镜像并启动

```powershell
# 登录服务器
ssh ubuntu@159.75.2.56

# 在服务器上执行：
cd /opt/xingqiu

# 加载镜像
docker load -i deploy/admin-web-prod.tar
docker load -i deploy/backend-prod.tar

# 启动全部服务（MySQL + Redis + Backend + Admin-Web）
docker compose -f docker-compose.prod.yml --env-file .env up -d

# 等 10 秒后检查状态
sleep 10
docker compose -f docker-compose.prod.yml ps
```

预期输出：4 个容器都是 `Up` 状态。

### 8.6 第六步：验证服务是否正常

```bash
# 在服务器上执行：

# 1. 检查容器状态
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 2. 检查后端是否启动成功
docker logs --tail=50 xq-backend

# 3. 测试后端接口（未登录返回 401 是正常的，说明后端在跑）
curl -I http://127.0.0.1:8328/api/v1/admin/auth/me

# 4. 测试管理网站
curl -I http://127.0.0.1:5137/
```

### 8.7 第七步：配置 Nginx 反向代理（域名访问）

如果你还没有域名或者只是想用 IP 访问，先跳过这一步。

在服务器上安装 Nginx，然后创建配置：

```bash
# 安装 Nginx
sudo tee /etc/nginx/sites-available/xingqiu << 'NGINX_EOF'
# 拦截 IP 直连和未知域名
server {
    listen 80 default_server;
    server_name _;
    return 444;
}

# 正式站点 — HTTP（证书申请前临时用，申请后 Certbot 会自动加上 HTTPS）
server {
    listen 80;
    server_name guanli.xgear.life;

    client_max_body_size 200m;

    # 安全响应头
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 管理后台
    location / {
        proxy_pass http://127.0.0.1:5137;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    # API 接口
    location /api/ {
        proxy_pass http://127.0.0.1:8328;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }
}
NGINX_EOF

# 启用站点
sudo ln -sf /etc/nginx/sites-available/xingqiu /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### 8.8 第八步：配置 HTTPS（重要！）

```bash
# 用 Certbot 自动获取免费 SSL 证书
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d guanli.xgear.life

# 按提示操作即可，Certbot 会自动：
#   - 申请并安装 SSL 证书
#   - 修改 Nginx 配置为 HTTPS
#   - 设置自动续期

# 手动检查续期是否正常
sudo certbot renew --dry-run
```

---

## 9. 上线前 10 项检查清单

在正式对外宣布上线之前，逐项打勾确认：

| # | 检查项 | 如何验证 |
|:---:|---|------|
| 1 | `.env` 全部占位符已替换 | 用记事本打开 `.env`，搜索"请"字，不应有任何结果 |
| 2 | `WX_APP_ID` 和 `WX_APP_SECRET` 正确 | 小程序能正常 wx.login 获取 code |
| 3 | `JWT_SECRET` 已换成强随机值 | 不是 `dev-secret-change-in-production` |
| 4 | `DB_PASSWORD` 已换成强密码 | 不是默认的 `xingqiu123` |
| 5 | `SPRINGDOC_ENABLED=false` | 你访问 `域名/swagger-ui.html` 返回 404 |
| 6 | 容器 4 个全 Up | `docker compose -f docker-compose.prod.yml ps` |
| 7 | 接口能正常调通 | 微信开发者工具打开小程序，能正常登录 + 首页加载 |
| 8 | 管理后台能正常打开 | 浏览器访问域名/服务器IP:5137，能看到页面 |
| 9 | 如果是 HTTPS，证书正常 | 浏览器地址栏显示小锁，无感叹号 |
| 10 | 支付功能（如有）已走通沙箱测试 | 微信支付商户平台的沙箱环境测试完成 |

---

## 10. 常见问题与排查

### Q1: 容器启动后 backend 马上退出

```bash
docker logs xq-backend --tail=100
# 看最后几行，通常是：
#   1. 连不上数据库 → 检查 DB_URL 里的 mysql 是不是写错了
#   2. 连不上 Redis  → 检查 REDIS_PASSWORD 是否正确
#   3. MySQL 还没启动完 → 等 30 秒重试，healthcheck 会处理
```

### Q2: 微信小程序登录失败

```bash
# 检查后端日志
docker logs xq-backend | grep -i "wechat\|wx\|appid"

# 常见原因：
#   1. WX_APP_ID / WX_APP_SECRET 填错了 → 回 §3.1 核对
#   2. 小程序没配置服务器域名白名单 → 去微信公众平台「开发 → 开发管理 → 服务器域名」
```

### Q3: 图片上传失败

```bash
# 检查后端日志
docker logs xq-backend | grep -i "cos\|upload"

# 常见原因：
#   1. COS_SECRET_ID / COS_SECRET_KEY 填错了 → 回 §5.1 核对
#   2. COS_BUCKET 填错了 → 回 §5.4 核对
#   3. COS 密钥没有 COS 的写入权限 → 腾讯云 CAM 里检查权限策略
```

### Q4: 支付回调收不到

```
# 常见原因：
#   1. WX_PAY_NOTIFY_URL 不是公网可访问的 → 微信支付后端没法访问你服务器
#   2. 不是 HTTPS → 微信支付强制要求 HTTPS
#   3. 域名没备案 → 微信要求域名已备案
#   4. 服务器防火墙没开 443 端口
```

### Q5: 怎么更新代码（第二次部署）

```bash
# 本地：重新编译 + 导出镜像（同 §8.2）
# 然后上传新 tar + .env（同 §8.4）

# 服务器上：
cd /opt/xingqiu
docker compose -f docker-compose.prod.yml down   # 停掉旧容器
docker load -i deploy/backend-prod.tar            # 加载新镜像
docker load -i deploy/admin-web-prod.tar
docker compose -f docker-compose.prod.yml up -d   # 启动新容器
```

### Q6: 我要回滚到上一个版本

```bash
cd /opt/xingqiu
docker compose -f docker-compose.prod.yml down
docker load -i deploy/backend-prod-backup.tar     # 用旧备份
docker load -i deploy/admin-web-prod-backup.tar
docker compose -f docker-compose.prod.yml up -d
```

---

> 📅 生成日期：2026-06-08
> 🤖 由 Claude Code 生成
> 📁 相关文件：`.env` / `docker-compose.prod.yml` / `deployment.md`
